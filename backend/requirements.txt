# LoveOS — وابستگی‌های بک‌اند
# نصب: python -m venv .venv && .venv/bin/pip install -r requirements.txt

# --------------------------------------------------------------- هسته ----
Django==5.2.17
djangorestframework==3.18.1
django-cors-headers==4.9.0
python-dotenv==1.2.3

# ------------------------------------------------------- رسانه و تاریخ ----
pillow==12.3.0            # پردازش عکس‌های خاطره، کاور آهنگ، پازل
jdatetime==6.1.0          # تاریخ جلالی در پنل بابا
jalali-core==1.0.0

# ------------------------------------------------ ارتباط با سروش و هوا ----
requests==2.34.2          # Soroush Bot API + Open-Meteo

# ------------------------------------------------------- پایگاه‌داده -----
# در توسعه SQLite استفاده می‌شود (DB_ENGINE=sqlite).
# برای پروداکشن MySQL این خط را از حالت کامنت خارج کنید:
# mysqlclient==2.2.7

# ------------------------------------- زمان‌بندی اختیاری (Celery/Redis) ---
# اگر DB_ENGINE=mysql و ردیس در دسترس بود، به‌جای cron از این‌ها استفاده کنید:
# celery==5.4.0
# redis==5.2.1
# django-celery-beat==2.7.0

# ------------------------------------------------------------ سرویس‌دهی --
# برای اجرای پروداکشن:
# gunicorn==23.0.0
# whitenoise==6.8.2
