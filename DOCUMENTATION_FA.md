<div dir="rtl">

# LoveOS — مستندات فنی و راهنمای کاربری

> یه سیستم‌عامل کوچولوی عاشقانه توی مرورگر.
> ساخته‌ی بابا، برای دخترش، از آن‌سوی فاصله‌ای که فقط یه عدده.

**نسخه:** ۲.۰ · **زبان پیش‌فرض:** فارسی · **زبان دوم:** انگلیسی
**نسخه‌ی انگلیسی این مستند:** [`DOCUMENTATION.md`](./DOCUMENTATION.md)

---

## فهرست

۱. [معرفی کلی](#۱-معرفی-کلی)
۲. [معماری](#۲-معماری)
۳. [نصب و راه‌اندازی](#۳-نصب-و-راهاندازی)
۴. [اپ‌های LoveOS](#۴-اپهای-loveos)
۵. [جستجوی سراسری](#۵-جستجوی-سراسری)
۶. [پنل بابا](#۶-پنل-بابا)
۷. [اتصال به بات سروش](#۷-اتصال-به-بات-سروش)
۸. [مرجع API](#۸-مرجع-api)
۹. [رازها و نشان‌ها](#۹-رازها-و-نشانها)
۱۰. [استقرار روی سرور](#۱۰-استقرار-روی-سرور)
۱۱. [نگهداری، تست و رفع اشکال](#۱۱-نگهداری-تست-و-رفع-اشکال)
۱۲. [تغییرات نسخه‌ی ۲.۰](#۱۲-تغییرات-نسخهی-۲۰)
۱۳. [لایه‌ی کیفیتِ سه‌بعدی](#۱۳-لایه‌ی-کیفیت-سهبعدی)
۱۴. [سه‌بعد در همه‌ی اپ‌ها](#۱۴-سهبعد-در-همه‌ی-اپها)

---

## ۱. معرفی کلی

### LoveOS چیست

LoveOS یک **اپ وب پیش‌رونده (PWA)** است که خودش را شبیه یک سیستم‌عامل کوچک نشان می‌دهد: بوت می‌شود،
رمز می‌پرسد، دسکتاپی با ویجت و داک نشان می‌دهد و ۳۰ اپ کوچک را در خودش جا داده — هر کدام یک راه
متفاوت برای گفتن «دوستت دارم» از آن‌سوی یک فاصله‌ی طولانی.

این پروژه هدیه‌ی من (بابا) به دخترم است. لحن همه‌جا گرم، بچگانه و بامزه است و همه‌ی متن‌ها مستقیم
با خود او حرف می‌زنند. از نسخه‌ی ۲.۰ پنج اپ تازه به سیستم اضافه شده که همه‌شان درباره‌ی «با هم بودن»
هستند: هماهنگ‌کردن تماس، دفتر هدیه‌ها، کتاب‌خوانی مشترک، خونه‌ی رویایی، پل زبان و جستجوی سراسری.

### اصول طراحی

| اصل | یعنی چه |
|---|---|
| **هیچ‌چیز هاردکد نیست** | تمام محتوا — پیام‌ها، ویس‌ها، نامه‌ها، عکس‌ها، سؤال‌های کوییز، جواب‌های ترمینال، حتی متن رازها — در پنل جنگو است و بدون دست زدن به کد عوض می‌شود. |
| **مخفی بودن از پایه** | اپ روی یک دامنه‌ی اعلام‌نشده است که فقط با QR پیدا می‌شود. `robots.txt` همه‌چیز را می‌بندد، همه‌ی پاسخ‌ها هدر `X-Robots-Tag: noindex` دارند و پنل پشت یک مسیر مخفی و یک رمز جداگانه است. |
| **اول موبایل، بعد دسکتاپ** | برای گوشیِ توی دست او طراحی شده؛ روی صفحه‌ی بزرگ اپ‌ها به پنجره‌های شناور و قابل جابه‌جایی تبدیل می‌شوند. |
| **دو زبان، صفر متن هاردکد** | همه‌ی رشته‌ها در `frontend/public/locales/{fa,en}/translation.json` هستند. فارسی پیش‌فرض است و چیدمان راست‌به‌چپ. |
| **کار کردن بدون اینترنت** | سرویس‌ورکر پوسته، فونت‌ها و آیکن‌ها را کش می‌کند تا حتی با اینترنت بد هم باز شود. |
| **یک حقیقت واحد برای موقعیت** | هر اپی که به «کجایی» نیاز دارد (نقشه، آب‌وهوا، ساعت، فاصله) از یک تابع مشترک می‌خواند؛ موقعیت زنده‌ی گوشی او بر مقدار ثبت‌شده‌ی پنل مقدم است. |
| **مهربان بودن به‌صورت پیش‌فرض** | صداها همه سنتز می‌شوند (بدون فایل کپی‌رایتی)، انیمیشن‌ها به `prefers-reduced-motion` احترام می‌گذارند و اپ حساس سلامت پیام سلب مسئولیت پزشکی دارد. |

### مسیر او در اپ

```
QR  →  دامنه‌ی مخفی
          │
          ▼
    ┌──────────┐   خط‌های بوت تایپ می‌شوند، ملودی نرم، «سلام دخترم...»
    │   بوت    │
    └────┬─────┘
         ▼
    ┌──────────┐   «امروز N روزه که بابا عاشقته.»
    │   قفل    │   رمز · سؤال امنیتی · بعد از ۵ بار اشتباه: «کمک از بابا»
    └────┬─────┘
         ▼
    ┌──────────┐   ساعت · شمارش معکوس دیدار · تماس بعدی · آب‌وهوای دو شهر
    │ دسکتاپ  │   ۲۹ آیکن · داک (با آیکن ذره‌بین جستجو) · منوی شروع · اعلان‌ها
    └────┬─────┘   Ctrl/⌘ + K = جستجوی سراسری
         ▼
       ۳۰ اپ
```

### ساختار پروژه

```
LoveOS/
├── backend/                     جنگو ۵ + DRF
│   ├── config/                  تنظیمات، urls ریشه، wsgi، asgi
│   ├── core/                    نوتیفیکیشن، صندوق خروجی سروش، دستاوردها،
│   │                            شمارنده‌ها، رازها، جستجوی سراسری، کش،
│   │                            موقعیت مؤثر، دستور sweep
│   ├── accounts/                UserConfig (تک منبع حقیقت)، نشست‌های دستگاه،
│   │                            موقعیت زنده، تلاش‌های قفل‌گشایی
│   ├── content/                 ویس‌ها، آهنگ‌ها، خاطره‌ها، نامه‌ها، شمارش معکوس،
│   │                            باغچه، صورت‌فلکی‌ها، سینما، کوییز، آرزوها،
│   │                            حال‌ودل، صندوقچه، آموزش، ترمینال
│   ├── social/                  چت، بغل، اعلان‌ها، یادآورها، آب‌وهوا، نقشه
│   ├── health/                  چرخه و مراقبت، داروها، یادآورهای مهربان
│   ├── library/                 «کتاب داستان ما» (نویسندگی مشترک)
│   ├── games/                   پازل قلب
│   ├── calls/                   اپ ۲۵ — هماهنگ‌کننده‌ی تماس
│   ├── gifts/                   اپ ۲۶ — دفتر هدیه‌ها
│   ├── reading/                 اپ ۲۷ — کتاب‌خوانی مشترک
│   ├── dreamhome/               اپ ۲۸ — خونه‌ی رویایی
│   └── language/                اپ ۲۹ — پل زبان
├── frontend/                    React ۱۹ + Vite ۸ + TS + Tailwind
│   ├── src/os/                  بوت، قفل، دسکتاپ، پنجره، داک، منوی شروع،
│   │                            مرکز اعلان، جستجوی سراسری، تم روز/شب
│   ├── src/apps/                ۳۰ فایل اپ (هر اپ یک فایل مستقل)
│   ├── src/shared/              store (Zustand)، api، ui، Icon، i18n،
│   │                            format، sound، geo، recorder، prefs،
│   │                            ErrorBoundary
│   ├── public/locales/          fa / en
│   └── vite.config.ts           پراکسی /api + سرویس‌ورکر PWA
├── scripts/                     اسکریپت‌های کمکی (QR، بکاپ، استقرار)
├── DOCUMENTATION.md             همین مستند (انگلیسی)
├── DOCUMENTATION_FA.md          همین مستند (فارسی)
├── QUICKSTART_FA.md             راه‌اندازی سریع روی سیستم خودم
├── DEPLOY_CPANEL_FA.md          استقرار روی هاست سی‌پنل
└── Soroush-Docs.md              مرجع API سروش‌پلاس
```

---

## ۲. معماری

### پشته‌ی فناوری

| لایه | چیزهایی که استفاده کرده‌ام |
|---|---|
| بک‌اند | Django 5.2 · Django REST Framework 3.18 · SQLite (توسعه) / MySQL (پروداکشن) |
| احراز هویت | Token دست‌ساز روی `DeviceSession` (بدون JWT)، `@require_session` |
| پنل | ادمین خود جنگو با مسیر مخفی + گیت رمز (`ADMIN_GATE_PASSCODE`) |
| فرانت‌اند | React ۱۹ · TypeScript ۶ · Vite ۸ · Tailwind ۳ · Framer Motion ۱۳ |
| وضعیت | Zustand (`src/shared/store.ts`) |
| چندزبانه | i18next + react-i18next (چیدمان RTL/LTR خودکار) |
| نقشه | MapLibre GL (با fallback سبک اگر WebGL نبود) |
| صدا | Web Audio API (سنتز) + Howler (پخش فایل‌های رسانه) |
| ضبط صدا | MediaRecorder + getUserMedia (`src/shared/recorder.ts`) |
| PWA | vite-plugin-pwa (Workbox) با `skipWaiting` و `clientsClaim` |

### مسیر یک درخواست

```
گوشی او ──► Vite (توسعه) / Nginx یا Passenger (پروداکشن)
              │
              ├─ /            → فایل‌های SPA (React)
              ├─ /api/...     → جنگو + DRF  →  مدل‌ها  →  JSON
              ├─ /api/media/... → رسانه‌ی خصوصی با URL امضاشده (ویس، عکس، PDF، صدای تلفظ)
              └─ /daddy-panel-9x7k/  → ادمین جنگو (فقط من)
```

در هر درخواست `Authorization: Token …` همراهش می‌رود. اگر نشست منقضی شود، پاسخ ۴۰۱ برمی‌گردد و
فرانت‌اند خودش قفل را بالا می‌آورد (رویداد `loveos:locked`).

### مدل احراز هویت

* `DeviceSession` — یک توکن ۳۲ کاراکتری با انقضا (`SESSION_TTL_HOURS`، پیش‌فرض ۷۲۰ ساعت = ۳۰ روز).
* `@require_session` — دکوراتوری که همه‌ی ویوهای خصوصی را می‌پوشاند.
* `UnlockAttempt` — هر تلاش رمز ثبت می‌شود؛ بعد از `MAX_UNLOCK_ATTEMPTS` (۵) دکمه‌ی «کمک از بابا» ظاهر می‌شود و همان لحظه به سروش من پیام می‌رود.
* سؤال امنیتی (`/api/auth/forgot`) و مسیر کمکی (`/api/auth/help`) راه‌های دوم و سوم باز کردن قفل هستند.
* صندوقچه (`/api/vault/unlock`) یک قفل جداگانه با تایمر کوتاه (`VAULT_SESSION_MINUTES`، پیش‌فرض ۲۰ دقیقه) دارد.

### ماشین مرحله‌ها

```
boot ──► lock ──► desktop
 ↑                  │
 └──── logout ──────┘
```

مرحله در `useOS` نگه داشته می‌شود و با `setPhase` عوض می‌شود. هر بار که به حالت غیر دسکتاپ برگردیم،
همه‌ی پنجره‌ها بسته و منوها بسته می‌شوند.

### مدیر پنجره (نسخه‌ی ۲.۰)

مدیر پنجره در این نسخه بازنویسی شد. سه قانون ساده اما حیاتی:

1. **مختصات فقط یک بار تعیین می‌شود.** هر پنجره وقتی باز می‌شود یک آبشار (`slot`) می‌گیرد و مختصاتش
   همان می‌ماند. هیچ کلیک/فوکوسی جای پنجره را عوض نمی‌کند — قبلاً همین باعث می‌شد پنجره از زیر انگشت
   بپرد و دکمه‌های بستن/مینیمایز «کار نکنند».
2. **لایه‌بندی روی پوشش بیرونی اعمال می‌شود.** `z-index` روی wrapper می‌نشیند نه روی جعبه‌ی متحرک؛ پس
   پنجره‌ی زیر هیچ‌وقت جلوی پنجره‌ی رو را نمی‌گیرد.
3. **پنجره‌ی محو‌شده کلیک نمی‌گیرد.** با `useIsPresent` وقتی انیمیشن بسته شدن تمام شد، پوشش
   `pointer-events: none` می‌شود؛ پس «شبحِ» پنجره‌ی بسته روی دسکتاپ نمی‌ماند.

روی موبایل هر اپ یک شیت تمام‌صفحه با هدر چسبان است و بدنه‌اش از زیر داک فاصله می‌گیرد؛ روی دسکتاپ
همان اپ یک پنجره‌ی شناور با دسته‌ی جابه‌جایی است. اندازه و جای هر پنجره به‌خاطر سپرده می‌شود و بار
بعد همان‌جا باز می‌شود.

### موقعیت مؤثر (نسخه‌ی ۲.۰)

قبلاً هر اپ یک جور با موقعیت کار می‌کرد و بعضی‌ها مقدار قدیمیِ ثبت‌شده در پنل را می‌خواندند. الان
یک تابع مشترک وجود دارد:

```python
# core/services.py
effective_daughter_location(cfg)  →  {lat, lng, city, timezone, is_live, captured_at, accuracy, source}
```

ترتیب اولویت:

1. **موقعیت زنده‌ی دستگاه او** اگر تازه باشد (کمتر از `location_ttl_minutes`) و من آن را در پنل نبسته باشم.
2. در غیر این صورت **مقدار ثبت‌شده در پنل بابا** (خانه/شهر پیش‌فرض).

نقشه، آب‌وهوا، فاصله، اختلاف ساعت و ویجت‌های دسکتاپ همه از همین تابع می‌خوانند. علاوه بر این، وقتی
گوشی‌اش موقعیت تازه می‌فرستد و جابه‌جایی معنادار است (بیشتر از `location_sync_km` یا تغییر شهر/منطقه‌ی
زمانی)، **مختصات و شهر ثبت‌شده در پنل هم خودکار به‌روز می‌شود** تا یک حقیقت واحد داشته باشیم و در
سروش هم خبرش را می‌گیرم.

### تم روز و شب

`src/os/daynight.ts` سه حالت را می‌فهمد: `auto` (بر اساس ساعت و طلوع/غروب شهر او)، `day` و `night`.
تم روی `data-theme` ریشه می‌نشیند و کل متغیرهای رنگ را عوض می‌کند؛ انتخاب کاربر در `localStorage`
و روی سرور (پروفایل) هم ذخیره می‌شود تا با هر ورود همان‌جا باشد.

---

## ۳. نصب و راه‌اندازی

### پیش‌نیازها

| ابزار | نسخه |
|---|---|
| Python | ۳٫۱۱ یا بالاتر |
| Node.js | ۲۰ یا بالاتر |
| npm | همراه Node |

### شروع سریع

```bash
git clone https://github.com/EmmettSS/LoveOS.git
cd LoveOS

# ---- بک‌اند
cd backend
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
cp .env.example .env
.venv/bin/python manage.py migrate
.venv/bin/python manage.py seed_loveos      # رمز ورود: 1234 · رمز صندوقچه: 0000
.venv/bin/python manage.py runserver 0.0.0.0:8000

# ---- فرانت‌اند (ترمینال دوم)
cd frontend
npm install
npm run dev     # http://localhost:5173
```

پراکسی Vite مسیرهای `/api`، `/static` و `/daddy-panel-9x7k` را به `127.0.0.1:8000` می‌فرستد،
پس هر دو ترمینال لازم است. راهنمای گام‌به‌گام با تصویر ذهنی هر مرحله در
[`QUICKSTART_FA.md`](./QUICKSTART_FA.md) آمده.

### دستورهای ادمین

```bash
.venv/bin/python manage.py makemigrations     # ساخت مهاجرت تازه
.venv/bin/python manage.py migrate            # اعمال مهاجرت‌ها
.venv/bin/python manage.py check              # بررسی سلامت پروژه
.venv/bin/python manage.py test               # ۹۷ تست بک‌اند
.venv/bin/python manage.py sweep              # کارهای زمان‌بندی‌شده (cron)
.venv/bin/python manage.py createsuperuser    # ساخت کاربر پنل (فقط بار اول)
```

### متغیرهای محیطی

فایل `backend/.env` (نمونه در `.env.example`):

| کلید | کار |
|---|---|
| `DEBUG` | حالت توسعه |
| `DJANGO_SECRET_KEY` | کلید امنیتی جنگو |
| `ALLOWED_HOSTS` | دامنه‌های مجاز |
| `CSRF_TRUSTED_ORIGINS` | مبدأهای مورد اعتماد |
| `TIME_ZONE` | منطقه‌ی زمانی سرور |
| `ADMIN_PATH` | مسیر مخفی پنل (پیش‌فرض `daddy-panel-9x7k`) |
| `ADMIN_GATE_PASSCODE` | رمز دوم پنل (اختیاری) |
| `DB_ENGINE` / `DB_NAME` / … | `sqlite` یا `mysql` |
| `NOTIFY_PROVIDER` | `console` · `soroush` · `null` |
| `SOROUSH_API_BASE` / `SOROUSH_TOKEN` / `SOROUSH_DADDY_CHAT_ID` | اتصال به ربات سروش |
| `SOROUSH_TIMEOUT` | تایم‌اوت هر درخواست سروش به ثانیه (پیش‌فرض `8`) |
| `NOTIFY_ASYNC` | ارسال اعلان در نخ پس‌زمینه تا هیچ درخواست API منتظر سروش نماند (پیش‌فرض `true`) |
| `SESSION_TTL_HOURS` | عمر نشست دستگاه او |
| `VAULT_SESSION_MINUTES` | چقدر صندوقچه باز بماند |
| `MAX_UNLOCK_ATTEMPTS` | چند بار رمز اشتباه تا «کمک از بابا» |
| `API_RATE_LIMIT` | سقف درخواست |
| `SERVE_FRONTEND` | اگر True باشد جنگو خودش فرانت‌اند را سرو می‌کند (cPanel) |

> ⚠️ هر بار `.env` را عوض کردی جنگو را ری‌استارت کن؛ فایل محیط فقط هنگام شروع خوانده می‌شود.

### ساخت QR برای او

```bash
.venv/bin/python ../scripts/make_qr.py https://love.example.com
```

---

## ۴. اپ‌های LoveOS

۳۰ اپ، که ۲۹ تا روی دسکتاپ آیکن دارند (اپ «درباره» فقط از منوی شروع باز می‌شود).

### هسته‌ی احساسی

| اپ | آیکن | چه‌کار می‌کند |
|---|---|---|
| **نقشه‌ی ما** | 🗺 | دو نقطه روی نقشه (خانه‌ی من، جای او)، فاصله، اختلاف ساعت، «پرواز روی مسیر» و نشان زنده بودن موقعیت. |
| **صندوق صدا** | 🎙 | آرشیو ویس‌های من، دسته‌بندی‌شده، با دکمه‌ی «یه ویس تصادفی». |
| **موسیقی ما** | 🎵 | آهنگ‌ها با «چرا این آهنگ» و متن؛ آپلود آهنگ تازه هم اگر اجازه داده باشم. |
| **خاطره‌ها** | 📸 | خاطره‌های مشترک، بعضی‌ها قفل‌شده تا تاریخ مشخص (رازِ خاطره‌ی آینده). |
| **نامه‌های نجوا** | ✉️ | نامه‌های دست‌نویس‌مانند با قلم نستعلیق. |
| **شمارش معکوس** | ⏳ | تا دیدار بعدی، تولد، سالگرد. |
| **آب‌وهوا** | ⛅ | هوای دو شهر کنار هم + یک جمله‌ی محبت‌آمیز بر اساس اختلاف دما. |
| **ضربان قلب** | 💓 | «قلبم برای تو می‌زند» — ضربان سنتزشده با هر ضربه‌ی دستش. |
| **باغچه‌ی ما** | 🌱 | گل‌ها را آب می‌دهد؛ هر آبیاری یک گل باز می‌کند. |
| **آسمان ستاره‌ها** | ✨ | صورت‌فلکی اسمش با ستاره کشیده می‌شود + ستاره‌های آرزو. |
| **چت با بابا** | 💬 | پیام مستقیم؛ پیام‌های کوتاه و صمیمی. |
| **حال دلم** | 🌈 | ثبت حال امروز؛ بر اساس حالش پیام مخصوص می‌بیند. |
| **کوییز ما** | 🧠 | کوییزهای من درباره‌ی خاطرات مشترک، با پاداش. |
| **آرزوهای ما** | 🎯 | لیست آرزوهای مشترک با دسته‌بندی و تیک «شد». |
| **سینمای ما** | 🎬 | لیست فیلم/سریال مشترک با وضعیت «دیدیم / می‌خوایم ببینیم». |
| **صندوقچه** | 🔐 | رازهای خصوصی با رمز جدا. |
| **بغل** | 🫂 | بغل فوری، رفت و برگشتی؛ وقتی ناخنک می‌زند به سروش من خبر می‌رسد. |

### مراقبت و روزمرگی

| اپ | آیکن | چه‌کار می‌کند |
|---|---|---|
| **چرخه و مراقبت** | 🌸 | ثبت پریود، علائم و داروها با پیام‌های مهربان و مرزهای پزشکی روشن. |
| **کتابخونه‌ی ما** | 📚 | «کتاب داستان ما» را با هم می‌نویسیم؛ فصل به فصل، با نظر روی هر پاراگراف. |
| **پازل قلب** | 🧩 | پازل تصویری با سه سطح آسان/متوسط/سخت و جمله‌ی پایانِ مخصوص. |
| **تنظیمات** | ⚙️ | زبان، تم، صدا، اندازه‌ی فونت، موقعیت زنده و راهنمای نصب PWA. |
| **آموزش** | 🎓 | درس‌های «سیستم‌عامل یعنی چی» که همه‌شان را خودم می‌نویسم. |
| **ترمینال** | ⌨️ | دستورهایی مثل `help`, `love`, `whoami` با جواب‌های بامزه. |
| **نشان‌ها** | 🏅 | ۳۲ دستاورد که با کارهای واقعی باز می‌شوند. |
| **درباره LoveOS** | 💗 | داستان پروژه و حرف آخر من. |

### با هم بودن (تازه‌های نسخه‌ی ۲.۰)

| اپ | آیکن | چه‌کار می‌کند |
|---|---|---|
| **هماهنگ‌کننده‌ی تماس** | 📞 | بازه‌های آزاد هفتگی هر دومان را می‌گذاریم؛ سیستم بازه‌های مشترک را پیدا می‌کند. تماس پیشنهاد می‌دهیم، تأیید/رد/جابه‌جا می‌کنیم و بعدش با مدت، حال‌وهوا و یادداشت ثبتش می‌کنیم. برای گزارش تماس می‌تواند همان‌جا با میکروفن جملات صوتی ضبط کند. |
| **دفتر هدیه‌ها** | 🎁 | هر هدیه‌ای که رد و بدل می‌شود ثبت می‌شود: مناسبت، قیمت (یا رنج قیمت)، عکس و «واکنشِ لحظه‌ی گرفتنش». آمار سالانه و نمودار می‌سازد. |
| **کتاب‌خوانی مشترک** | 📖 | قفسه‌ی مشترک با کتاب‌های «کتابخونه»؛ برای هر فصل یادداشت و ستاره، نقل‌قول‌های ماندگار و گفتگوی زیر هر فصل. درصد پیشرفت هر دو طرف جداجدا نشان داده می‌شود. |
| **خونه‌ی رویایی** | 🏡 | چک‌لیست آرزوهای خانه با درجه‌ی اهمیت، نقشه‌ی اتاق‌ها (کشیدن و جابه‌جا کردن روی نقشه) و گالری عکس الهام با کامنت. |
| **پل زبان** | 💬 | دیکشنری چهارزبانه‌ی ما (مازندرانی، ترکی، فارسی، انگلیسی)، فلش‌کارت با شمارنده‌ی روزهای پشت‌سرهم، ضبط تلفظ با میکروفن، و کوییزهایی که فقط از پنل من می‌آید. |

### سیستم

| بخش | چه‌کار می‌کند |
|---|---|
| **بوت** | خط‌های تایپ‌شونده، لوگو، ملودی نرم، پیام خوش‌امدگویی من. |
| **قفل** | رمز + سؤال امنیتی + دکمه‌ی «کمک از بابا». |
| **دسکتاپ** | ساعت، شمارش معکوس دیدار، تماس بعدی، آب‌وهوای دو شهر، پیام امروز و آیکن‌های اپ‌ها. |
| **داک** | منوی شروع، اپ‌های باز، جستجو (ذره‌بین)، اعلان‌ها، تنظیمات، خروج. |
| **مرکز اعلان‌ها** | همه‌ی اعلان‌های سمت او با شمارنده‌ی خوانده‌نشده. |
| **جعبه‌ی محافظ خطا** | اگر بخشی از یک اپ به مشکل بخورد، کل سیستم‌عامل سفید نمی‌شود؛ همان اپ پیام «یه بار دیگه امتحان کن» نشان می‌دهد. |

---

## ۵. جستجوی سراسری

جستجوی سراسری یک اپ جدا نیست؛ یک لایه‌ی روی همه‌ی سیستم است.

**سه راه باز کردنش:**

* `Ctrl + K` یا `⌘ + K` از هر جای LoveOS
* آیکن ذره‌بین در داک
* ردیف «جستجوی سراسری» در بالای منوی شروع

**۲۱ منبعی که می‌گردد:** پیام‌ها · نامه‌ها · ویس‌ها · خاطره‌ها · موسیقی · کتاب‌های داستان ما ·
فصل‌ها · پاراگراف‌ها · هدیه‌ها · ویژگی‌های خانه · اتاق‌های خانه · کلمه‌های پل زبان · کتاب‌های مشترک ·
یادداشت‌های فصل · نقل‌قول‌ها · سؤال‌های کوییز · آرزوها · دستاوردها · تماس‌های ما · قرارهای تماس · رازها.

**نکته‌های پیاده‌سازی:**

* **غلط‌گیری فارسی:** «ی/ك» عربی، اعراب و نیم‌فاصله قبل از مقایسه یکسان می‌شوند؛ اگر تطابق دقیق پیدا
  نشود، تطابق فازی (شباهت ≥ ۰٫۶۲) امتحان می‌شود. یعنی «كتاب يادگاري» همان «کتاب یادگاری» را می‌آورد.
* **فیلترها:** روی هر اپ/منبع، روی بازه‌ی تاریخ (`from`/`to`) و روی نوع رکورد می‌شود فیلتر کرد.
* **حالت خالی:** به‌جای «چیزی پیدا نشد»، پیشنهادهای واقعی (آخرین پیام، آخرین خاطره، آخرین کتاب…) نشان داده می‌شود.
* **کش:** نتیجه‌ی هر کوئری ۴۵ ثانیه کش می‌شود (کلید کش شامل همه‌ی ورودی‌هاست) تا تایپ کردن روان باشد؛
  در پنل هم یک اکشن «پاک‌کردن کش» دارم.
* **لاگ اختیاری:** اگر در پنل روشن کنم، فقط متن کوئری در `ActivityLog` ذخیره می‌شود (بدون هیچ محتوای دیگری).
* **رازها** عمداً فقط با «عنوان» پیدا می‌شوند، نه با متن؛ نمی‌خواهم رازها لو بروند.

---

## ۶. پنل بابا

### ورود

```
https://دامنه/daddy-panel-9x7k/
```

* مسیر از `ADMIN_PATH` می‌آید و در پروداکشن عوضش می‌کنم.
* یک گیت رمز دوم هم دارد (`ADMIN_GATE_PASSCODE`) که با سشن کار می‌کند.
* همه‌ی آمار فعالیت‌ها در `ActivityLog` ثبت می‌شود و در پنل فیلترپذیر است.

### چه چیزهایی را کنترل می‌کنم

| بخش پنل | چیزهایی که عوض می‌کنم |
|---|---|
| **UserConfig** | اسم و لقب من و او، پیام‌های بوت/قفل، سؤال امنیتی، تم و زبان پیش‌فرض، فونت، پس‌زمینه‌های بوت/قفل/دسکتاپ، تاریخ تولد و سالگرد، شهر و مختصات هر دو طرف، تنظیمات موقعیت زنده (فعال/غیرفعال، عمر مجاز، فاصله‌ی هم‌آهنگ‌سازی)، فیلدستِ «جستجوی سراسری» (خاموش‌کردن منابع و لاگ کوئری) |
| **LiveLocation** | موقعیت زنده‌ی فعلی او، دقت، منبع، سن ثبت — برای وقتی که بخواهم دستی تصحیح کنم |
| **تماس‌ها** | بازه‌های آزاد، قرارها، دفتر تماس‌ها و «تنظیمات تماس» (پیش‌فرض مدت، متن قالب‌های پیشنهاد/تأیید/رد، روشن/خاموش بودن نوتیف‌ها و یادآور) |
| **هدیه‌ها** | مناسبت‌ها (با آیکن) و رکورد هدیه‌ها با رنج قیمت، عکس و برچسب محبوب |
| **کتاب‌خوانی** | کتاب‌ها، فصل‌ها، یادداشت‌ها، نقل‌قول‌ها و کامنت‌ها — همه با اینلاین |
| **خونه‌ی رویایی** | دسته‌بندی‌ها، ویژگی‌ها (با پیش‌نمایش عکس و چیپ رنگ)، اتاق‌های نقشه با اینلاین ایده‌ها و گالری الهام با کامنت‌ها |
| **پل زبان** | دسته‌ها، کلمه‌ها/اصطلاح‌ها با ترجمه‌ها، سؤال‌های کوییز و پیشرفت هر دو نفر |
| **محتوا** | ویس، آهنگ، خاطره، نامه، شمارش، باغچه، صورت‌فلکی، سینما، کوییز، آرزو، حال‌ودل، صندوقچه، آموزش، دستورهای ترمینال |
| **دستاوردها و رازها** | ۳۲ نشان با آستانه و پیام مخفی، ۱۵ راز با متن و جای فعال‌سازی |
| **سلامت** | داروها، یادآورهای مراقبت، پیام‌های حال‌ودل |
| **سروش** | صندوق خروجی (`SoroushOutbox`) با وضعیت ارسال/تلاش‌ها/خطا و دکمه‌ی ارسال دوباره |
| **ActivityLog** | همه‌ی فعالیت‌ها + اکشن «پاک‌کردن کش اپ‌ها» |

### دستورالعمل‌های روزمره

| می‌خواهم… | چه‌کار می‌کنم |
|---|---|
| یک ویس تازه بگذارم | محتوا → ویس‌ها → افزودن → فایل صوتی + دسته + عنوان |
| پیام امروز دسکتاپ را عوض کنم | UserConfig → «پیام امروز» |
| خاطره‌ی یک تاریخ خاص قفل شود | خاطره‌ها → «خاطره‌ی آینده» + زمان باز شدن → `sweep` (کرون) خودش بازش می‌کند |
| بگویم یادش نرود قرصش را بخورد | داروها و یادآورهای مهربان → ساعت و متن → `sweep` هر ۱۵ دقیقه چک می‌کند |
| موقعیت ثبت‌شده‌اش را دستی درست کنم | Location → شهر/مختصات |
| جستجو را سبک‌تر کنم | UserConfig → «جستجوی سراسری» → منابعی که نمی‌خواهم نتیجه بدهند را انتخاب کنم |
| ببینم امروز چه‌کار کرده | ActivityLog (فیلتر بر اساس اپ) |

---

## ۷. اتصال به بات سروش

### راه‌اندازی

```ini
NOTIFY_PROVIDER=soroush
SOROUSH_API_BASE=https://api.splus.ir
SOROUSH_TOKEN=توکنِ-بات
SOROUSH_DADDY_CHAT_ID=آیدیِ-چتِ-من
SOROUSH_WEBHOOK_SECRET=یه-رشته-تصادفی
SOROUSH_PARSE_MODE=HTML
```

* همه‌ی پیام‌ها اول در `SoroushOutbox` ثبت می‌شوند و **بعد** تلاش ارسال می‌شود؛ پس اگر اینترنت/توکن
  خراب باشد هیچ پیامی گم نمی‌شود. `manage.py sweep` هر بار صف را دوباره می‌فرستد
  (تا `OUTBOX_MAX_RETRY`، پیش‌فرض ۵ تلاش).
* وبهوک ورودی: `/api/soroush/webhook/<SOROUSH_WEBHOOK_SECRET>/` — با آن از سروش می‌توانم پیام بفرستم
  و اپ هم جواب می‌دهد.
* **هیچ‌وقت به سمت او پیام نمی‌رود**؛ سروش فقط کانال من است. اعلان‌های سمت او داخل اپ
  (`OSNotification`) ساخته می‌شوند.

### رویدادهایی که برایم می‌آید

| گروه | رویدادها |
|---|---|
| ورود و قفل | `login` · `lock_help` · `forgot_ok` |
| پیام و بغل | `chat` · `hug` · `hug_seen` · `hug_miss` |
| محتوا | `voice_played` · `song_uploaded` · `letter_opened` · `memory_unlocked` · `mood` · `easter_egg` |
| کوییز و بازی | `quiz_perfect` · `puzzle` · `achievement` |
| سلامت | `med_added` · `med_taken` · `med_skipped` · `symptoms` · `period_start` · `period_end` · `cycle_anomaly` |
| یادآورها | `reminder_sent` · `reminder_viewed` |
| کتاب و فیلم | `book_new` · `book_chapter` · `book_comment` · `cinema_add` · `plan_add` · `plan_done` |
| **تماس** | `call_proposed` · `call_approved` · `call_rejected` · `call_rescheduled` · `call_logged` · `call_reminder` |
| **هدیه** | `gift_added` |
| **کتاب‌خوانی** | `reading_book` · `reading_note` · `reading_quote` · `reading_comment` |
| **خونه‌ی رویایی** | `home_room` · `home_feature` · `home_idea` · `home_inspiration` · `home_comment` |
| **پل زبان** | `language_entry` · `language_quiz` · `language_practice` |
| **موقعیت** | `location_change` |

---

## ۸. مرجع API

همه‌ی مسیرهای خصوصی به هدر `Authorization: Token <token>` نیاز دارند. پاسخ‌ها JSON هستند.

### پوسته و احراز هویت

| متد | مسیر | کار |
|---|---|---|
| GET | `/api/boot` | تنظیمات عمومی + وضعیت قفل (بدون راز) |
| POST | `/api/auth/unlock` | باز کردن قفل با رمز → توکن نشست |
| POST | `/api/auth/forgot` | پاسخ سؤال امنیتی → باز شدن قفل |
| POST | `/api/auth/help` | «کمک از بابا» → پیام فوری به سروش من |
| POST | `/api/auth/logout` | بستن نشست |
| GET | `/api/me` | پیکربندی کامل بعد از قفل |
| GET/POST/PATCH | `/api/settings` | زبان، تم، صدا، اندازه‌ی فونت |
| GET/POST | `/api/location` | خواندن موقعیت مؤثر / ثبت موقعیت تازه‌ی دستگاه |
| GET | `/api/search` | جستجوی سراسری (`q`, `app`, `kind`, `from`, `to`, `suggest`) |
| POST | `/api/vault/unlock` | باز کردن صندوقچه |
| GET | `/healthz` | سلامت سرویس |

### محتوا

`/api/voices` (`…/random`, `…/<id>/played`) · `/api/songs` (`…/upload`, `…/<id>`, `…/<id>/played`) ·
`/api/memories` · `/api/letters` (`…/random`, `…/<id>/open`) · `/api/countdowns` · `/api/garden`
(`…/<id>/water`) · `/api/starmap` · `/api/cinema` (`…/<id>`) · `/api/quiz` (`…/submit`) · `/api/plans`
(`…/<id>`) · `/api/moods` (`…/set`) · `/api/vault` · `/api/tutorial` · `/api/terminal` (`…/sudo`) ·
`/api/egg` · `/api/achievements`

### اجتماعی

`/api/chat` · `/api/hug` (`…/send`, `…/<id>/open`) · `/api/notifications` (`…/read-all`, `…/<id>/read`) ·
`/api/reminders` (`…/<id>/mute`) · `/api/weather` · `/api/map`

### سلامت

`/api/cycle` (`…/start`, `…/end`, `…/symptoms`) · `/api/meds` (`…/today`, `…/act`, `…/report`) ·
`/api/care` (`…/<id>/toggle`)

### کتابخانه و بازی

`/api/books` (`…/<id>`, `…/<book_id>/chapters`) · `/api/chapters/<id>/publish` ·
`/api/pages/<page_id>/paragraphs` · `/api/pages/<page_id>/bookmark` · `/api/paragraphs/<id>` ·
`/api/paragraphs/<paragraph_id>/notes` · `/api/puzzles` (`…/<id>/start`, `…/<id>/complete`)

### تماس (`/api/calls`)

| متد | مسیر | کار |
|---|---|---|
| GET | `/api/calls/overview` | همه‌ی چیزی که اپ لازم دارد در یک درخواست |
| GET/POST | `/api/calls/slots` | بازه‌های آزاد + بازه‌های مشترک |
| POST/DELETE | `/api/calls/slots/<id>` | حذف بازه |
| GET/POST | `/api/calls/appointments` | فهرست/پیشنهاد تماس |
| POST | `/api/calls/appointments/<id>/respond` | تأیید، رد یا پیشنهاد جایگزین |
| POST/DELETE | `/api/calls/appointments/<id>/cancel` | لغو |
| GET/POST | `/api/calls/logs` | دفتر تماس‌ها (با ضمیمه‌ی صوتی: `multipart`) |
| PATCH/POST | `/api/calls/logs/<id>` | ویرایش گزارش |
| GET | `/api/calls/stats` | آمار ماه، شش ماه، رکورد و میانگین |
| GET | `/api/calls/next` | تماس بعدی (برای ویجت دسکتاپ) |

### هدیه‌ها (`/api/gifts`)

`GET/POST /api/gifts` · `GET/PATCH/POST/DELETE /api/gifts/<id>` · `GET /api/gifts/occasions` ·
`GET /api/gifts/stats` — فیلترها: `giver`, `receiver`, `occasion`, `year`, `band`, `q`.

### کتاب‌خوانی مشترک (`/api/reading`)

`GET /api/reading/overview` · `GET/POST /api/reading/books` · `GET/PATCH/POST/DELETE /api/reading/books/<id>` ·
`POST /api/reading/books/<id>/chapters` · `POST /api/reading/books/<id>/progress` ·
`POST/GET /api/reading/chapters/<id>/notes` · `POST/GET /api/reading/chapters/<id>/quotes` ·
`POST /api/reading/chapters/<id>/comments` · `GET /api/reading/quotes` · `GET /api/reading/stats`.

برای وصل‌کردن یک کتابِ اپ «کتابخونه» به قفسه‌ی مشترک، همان `library_book` را در `POST /books` بفرستید
(اگر از قبل وصل باشد همان رکورد برمی‌گردد و `already: true` می‌گیرید).

### خونه‌ی رویایی (`/api/home`)

`GET /api/home/overview` · `GET/POST /api/home/features` · `PATCH/POST/DELETE /api/home/features/<id>` ·
`GET/POST /api/home/rooms` · `PATCH/POST/DELETE /api/home/rooms/<id>` ·
`POST/GET /api/home/rooms/<id>/ideas` · `GET/POST /api/home/inspirations` ·
`GET/POST/DELETE /api/home/inspirations/<id>` · `POST/GET /api/home/inspirations/<id>/comments` ·
`GET /api/home/categories` · `GET /api/home/stats` — مختصات اتاق‌ها درصدی است (۰..۱۰۰) و در سرور
به بازه‌ی مجاز محدود می‌شود.

### پل زبان (`/api/language`)

`GET /api/language/overview` · `GET/POST /api/language/entries` ·
`GET/PATCH/POST/DELETE /api/language/entries/<id>` (آپلود تلفظ با فیلد `pronunciation`) ·
`GET /api/language/categories` · `GET /api/language/flashcards` ·
`POST /api/language/practice` · `GET /api/language/quiz` · `POST /api/language/quiz/submit` ·
`GET /api/language/stats`.

### بیرون از `/api`

| مسیر | کار |
|---|---|
| `/` | خود LoveOS (SPA) |
| `/daddy-panel-9x7k/` | پنل بابا |
| `/api/media/<path>?sig=…` | فایل خصوصی با URL امضاشده و کوتاه‌عمر |
| `/robots.txt` | بستن همه‌ی خزنده‌ها |

---

## ۹. رازها و نشان‌ها

### ۱۵ راز

| راز | کجا | چه اتفاقی می‌افتد |
|---|---|---|
| آسمون نیمه‌شب | بین ۰۰:۰۰ تا ۰۵:۰۰ | آسمان پر از قلب و ستاره می‌شود |
| قلب سالگرد | روز سالگرد | قلب شناور روی دسکتاپ |
| کیک تولد | روز تولدش | آیکن مخفی کیک |
| جمله‌ی جادویی | نوشتن «دوستت دارم» در چت | بارون قلب |
| کد قدیمی | کونامی روی دسکتاپ | پیام مخفی |
| ساندویچ | `sudo make sandwich` در ترمینال | جواب بامزه |
| حذف تنهایی | `rm -rf tanhayi` | پیام مخصوص |
| بارون قلب | تایپ‌کردن «love» | قلب روی صفحه |
| کمک بابا | ۵ بار رمز اشتباه | پیام کمک از طرف من |
| قلب پنهان لوگو | ۵ بار کلیک روی لوگو | قلب پنهان |
| ستاره‌ی مرکزی | دوبار کلیک روی ستاره | آرزوی مخصوص |
| خونه‌ی بابا | زوم زیاد روی نقشه | پیام خانه |
| آهنگ ما | سه بار پخش یک آهنگ | پیام آهنگ ما |
| دلتنگی | سه بغل پشت‌سرهم | پیام دلتنگی |
| گل مخصوص | پنج بار آبیاری یک گل | گل ویژه |

### نشان‌ها (۳۲ تا)

*۱۶ نشان قدیمی:* اولین ورود · اولین ویس · ۱۰۰ روز با بابا · کوییز کامل · همه نامه‌ها · پازل‌باز ·
استاد پازل · جوجه کوچولوی بغلی بابا · دختر مهربون بابا · منظم · مراقب خودت · نویسنده · داستان‌سرا ·
کاشف رازها · استاد رازها · همه‌چیزدان

*۱۶ نشان تازه‌ی نسخه‌ی ۲.۰:* اولین تماس · ده تماس · صد ساعت تماس · اولین هدیه · ده هدیه · پنجاه هدیه ·
اولین کتاب مشترک · پنج کتاب · ده کتاب · صد یادداشت · ده ویژگی خانه · پنجاه ویژگی خانه · اولین اتاق ·
اولین کلمه · صد کلمه · استاد زبان

همه‌ی آستانه‌ها از پنل قابل تغییرند و متن مخفی هر نشان هم همان‌جاست. بعد از باز شدن، اعلان داخل اپ +
پیام سروش برای من فرستاده می‌شود.

---

## ۱۰. استقرار روی سرور

### با یک دستور

```bash
./scripts/deploy.sh
```

اسکریپت وابستگی‌ها را نصب می‌کند، جمع‌آوری استاتیک انجام می‌دهد، فرانت‌اند را بیلد می‌کند، مهاجرت‌ها را
اعمال می‌کند و سرویس را ری‌استارت می‌کند. راهنمای کامل هاست سی‌پنل در
[`DEPLOY_CPANEL_FA.md`](./DEPLOY_CPANEL_FA.md) است.

### چک‌لیست پروداکشن

- [ ] `DEBUG=False`
- [ ] `DJANGO_SECRET_KEY` طولانی و تصادفی
- [ ] `ALLOWED_HOSTS` فقط دامنه‌ی خودم
- [ ] `ADMIN_PATH` را عوض کنم + `ADMIN_GATE_PASSCODE` بگذارم
- [ ] `SERVE_FRONTEND=True` اگر روی هاست تک‌اپی هستم
- [ ] `NOTIFY_PROVIDER=soroush` با توکن واقعی
- [ ] `python manage.py check --deploy`
- [ ] `python manage.py migrate` و بعد `python manage.py collectstatic --noinput`
- [ ] `cd frontend && npm run build`
- [ ] کرون برای `manage.py sweep` (هر ۱۵ دقیقه)
- [ ] بکاپ روزانه از دیتابیس و پوشه‌ی `media`

> از نسخه‌ی ۲.۰ پنج مهاجرت تازه وجود دارد (`calls`, `gifts`, `reading`, `dreamhome`, `language`)
> به‌علاوه‌ی `accounts.0003`. اگر از نسخه‌ی قبل آپدیت می‌کنی، قبل از هر چیز `migrate` را بزن.

### MySQL

در `.env` مقدار `DB_ENGINE=mysql` و بقیه‌ی کلیدهای `DB_*` را پر کن. هیچ تغییری در کد لازم نیست.

### زمان‌بندی (کرون)

```cron
*/15 * * * * cd /home/USER/loveos/backend && .venv/bin/python manage.py sweep >> /home/USER/loveos-sweep.log 2>&1
```

`sweep` این کارها را انجام می‌دهد: باز کردن خاطره‌های زمان‌دار، آزاد کردن نامه‌های آینده، یادآوری
داروها و مراقبت، ارسال یادآورها، **یادآوری تماس‌های تأییدشده** و خالی‌کردن صف سروش.

---

## ۱۱. نگهداری، تست و رفع اشکال

### کارهای دوره‌ای

| هر چند وقت | چه‌کار |
|---|---|
| روزانه | نگاه کردن به `ActivityLog` و صف سروش |
| هفتگی | گرفتن بکاپ دیتابیس + `media/`، چک کردن فضای دیسک |
| ماهانه | `pip list --outdated` و `npm outdated`، بازبینی محتوای پنل |
| هر بار آپدیت | `migrate` → `npm run build` → ری‌استارت سرویس |

### تست‌ها

```bash
# بک‌اند: ۹۷ تست (مدل‌ها، API‌ها، جستجو، موقعیت، پنل، صورت‌های فلکی، کیفیتِ UI)
cd backend && .venv/bin/python manage.py test

# فرانت‌اند: تایپ‌چک، لینت، بیلد و ۹ سوئیتِ رابط کاربری (۲۸۵ بررسی)
cd frontend && npx tsc -b && npx oxlint src && npm run build && npm run test:ui
```

تست‌های بک‌اند این‌ها را قفل می‌کنند: بازه‌های مشترک تماس، تأیید/رد/جابه‌جایی قرار، یادآوری یک‌بار‌مصرف
تماس، آمار هدیه‌ها و رنج قیمت، درصد پیشرفت کتاب و قفسه‌ی مشترک، محدود ماندن مختصات اتاق‌ها،
فلش‌کارت و streak زبان و اینکه کوییز فقط از سؤال‌های پنل می‌آید، غلط‌گیری فارسی در جستجو،
موقعیت مؤثر (زنده در برابر پنل و موقعیت کهنه) و اینکه همه‌ی مدل‌ها در پنل بدون خطا باز می‌شوند.

### آزمون‌های رابط کاربری (بدون مرورگر)

برای اینکه برای تست رابط کاربری نیازی به باز کردن مرورگر نباشد، این آزمون‌ها را با **jsdom** نوشتم: هر فایل
`frontend/tests/*.tsx` با esbuild باندل می‌شود و بعد مثل یک برنامه‌ی Node اجرا می‌شود.

```bash
cd frontend
npm run test:ui            # هر دو فایل آزمون
npm run test:ui window-manager-settings
```

| فایل | چه چیزی را قفل می‌کند |
|---|---|
| `tests/window-manager-settings.tsx` | باز/بسته/مینیمایز شدن پنجره، ثابت‌ماندن هندسه در زمان کلیک، درست بودن z-index، نماندن «شبح» پنجره‌ی بسته، برگشتن به موقعیت قبلی، و اعمال+ذخیره‌ی زبان و تم |
| `tests/apps-render.tsx` | بالا آمدن هر پنج اپ تازه با پاسخ‌های واقعی بک‌اند (`tests/fixtures.json`)، جستجوی سراسری، آیکن‌های دسکتاپ و ویجت تماس بعدی |

پاسخ‌های بک‌اند در `tests/fixtures.json` از خروجی واقعی APIها گرفته شده‌اند؛ پس اگر قرارداد یک
اندپوینت عوض شود، همین آزمون‌ها زودتر از خودِ اپ خبر می‌دهند.

### مشکلات رایج

| نشانه | علت احتمالی | راه‌حل |
|---|---|---|
| «اپ باز می‌شود ولی صفحه خالی است» | نسخه‌ی قدیمی سرویس‌ورکر | سرویس‌ورکر با `skipWaiting`/`clientsClaim` خودش را به‌روز می‌کند؛ اگر باز هم دیدی یک بار `Ctrl+Shift+R` بزن. برای همیشه: `npm run build` تازه روی سرور |
| دکمه‌ی بستن/مینیمایز پنجره کار نمی‌کند | مختصات پنجره در حال تغییر بوده | از نسخه‌ی ۲.۰ مختصات قفل است؛ اگر باز هم دیدی کنسول را نگاه کن که `AppWindow` بدون `z-index` روی wrapper رندر شده باشد |
| اعداد و حروف آینه‌ای دیده می‌شوند | تبدیل محور y در نقشه‌ی ستاره‌ها | از نسخه‌ی ۲.۰ محور y برگردانده شده و ترتیب حروف قابل انتخاب است (راست‌به‌چپ / چپ‌به‌راست) |
| موقعیت/ساعت/آب‌وهوا قدیمی است | مقدار ثبت‌شده‌ی پنل که موقعیت زنده‌ی کهنه را نگرفته | در تنظیمات او، «موقعیت زنده» را روشن کن؛ یا در پنل مقدار تازه بگذار |
| `/api/settings` خطای ۴۰۵ می‌دهد | متد پشتیبانی‌نشده | از نسخه‌ی ۲.۰ روی `GET`/`POST`/`PATCH`/`PUT` جواب می‌دهد |
| پیام‌های سروش نمی‌رسد | توکن/اینترنت | صف را ببین: `SoroushOutbox` — هر ردیف وضعیت و خطا دارد؛ بعد `manage.py sweep` |
| پس از آپدیت، صفحه‌ی سفید | فایل‌های بیلد قدیمی در کش مرورگر | بیلد تازه + یک بار رفرش سخت |
| پنل بالا نمی‌آید | `ADMIN_PATH` یا گیت رمز | مسیر درست را از `.env` بخوان و اگر گیت روشن است رمز را وارد کن |

### کوئری‌های مفید

```bash
# امروز چه‌کار کرده؟
.venv/bin/python manage.py shell -c "
from core.models import ActivityLog
[print(a.created_at, a.title, a.detail) for a in ActivityLog.objects.all()[:20]]"

# کدام رازها را پیدا کرده؟
.venv/bin/python manage.py shell -c "
from core.models import EasterEggLog
[print(l.created_at, l.egg.title) for l in EasterEggLog.objects.all()]"

# آخرین موقعیت زنده‌اش
.venv/bin/python manage.py shell -c "
from accounts.models import LiveLocation
l = LiveLocation.current()
print(l.city, l.lat, l.lng, l.age_minutes(), 'دقیقه قبل') if l else print('ثبت نشده')"

# صف سروش را به زور خالی کن
.venv/bin/python manage.py shell -c "
from core.soroush import flush_outbox; print(flush_outbox())"

# کش جستجو را پاک کن
.venv/bin/python manage.py shell -c "
from core.services import clear_app_cache; print(clear_app_cache())"
```

---

## ۱۲. تغییرات نسخه‌ی ۲.۰

### باگ‌هایی که رفع شد

1. **پنجره‌های بسته‌شده دوباره باز نمی‌شدند.** مدیر پنجره بازنویسی شد: مختصات در لحظه‌ی باز شدن
   تعیین و بعد ثابت می‌شود، لایه‌بندی روی پوشش بیرونی می‌نشیند و پنجره‌ی در حال محو شدن دیگر کلیک
   نمی‌گیرد.
2. **حروف در آسمان ستاره‌ها آینه‌ای بودند.** تابع محور y برعکس شد (قبلاً `M` شبیه `W` دیده می‌شد) و
   ترتیب حروف در چیدمان راست‌به‌چپ درست شد؛ حالا با یک دکمه هم قابل عوض کردن است.
3. **موقعیت جغرافیایی از مقدار قدیمی حساب می‌شد.** از این پس همه‌ی اپ‌ها از «موقعیت مؤثر» می‌خوانند
   که اولویت اولش موقعیت زنده‌ی دستگاه اوست؛ علاوه بر آن مختصات/شهر ثبت‌شده در پنل هم خودکار
   هم‌آهنگ می‌شود.
4. **اپ‌های دسکتاپ «کار نمی‌کردند».** علت مشترک، پوسته‌ی کش‌شده‌ی قدیمی PWA و بارگذاری‌نشدن چانک‌ها
   بود: `skipWaiting`، `clientsClaim` و `cleanupOutdatedCaches` فعال شد و رویداد `vite:preloadError`
   یک رفرش تازه می‌زند. همچنین یک «جعبه‌ی محافظ خطا» دور هر اپ گذاشته شد تا خطای یک اپ کل
   سیستم‌عامل را سفید نکند.
5. **کلید تم تنظیمات اثری نداشت.** تم حالا روی سرور و دستگاه ذخیره می‌شود، بلافاصله اعمال می‌شود و
   با `auto/day/night` کار می‌کند.
6. **تغییر زبان فقط جهت را عوض می‌کرد.** همه‌ی متن‌های اپ‌های تازه و پرالت‌ها دوزبانه شدند و پس از
   تغییر زبان، کل رابط (نه فقط جهت) عوض می‌شود.
7. **`PATCH /api/settings` خطای ۴۰۵ می‌داد.** این ویو الان `GET`، `POST`، `PATCH` و `PUT` را می‌پذیرد و
   مقادیر نامعتبر را بی‌صدا رد می‌کند (نه اینکه ۵۰۰ بدهد).
8. **بازه‌ی تاریخ در جستجوی سراسری دقیق نبود.** فیلتر «از تاریخ … تا تاریخ …» هم مقدار تاریخِ خام را
   به فیلدهای زمانی می‌داد (و Django هشدار naive datetime می‌داد) و هم در مسیر «تطبیق نرم» نادیده
   گرفته می‌شد؛ یعنی نتیجه‌ی بیرون از بازه می‌توانست بالا بیاید. الان مرزهای بازه با timezone حساب
   می‌شوند، «تا این تاریخ» همه‌ی همان روز را شامل می‌شود و هر دو مسیر جستجو (دقیق و نرم) همان
   بازه را رعایت می‌کنند.
9. **اگر اجازه‌ی میکروفن رد می‌شد، هیچ پیامی نمی‌آمد.** حالا در «هماهنگ‌کننده‌ی تماس» و «پل زبان»
   متن روشنی می‌گوید دسترسی میکروفن داده نشده و باید از تنظیمات مرورگر اجازه بدهد.

### چیزهای تازه

* **پنج اپ تازه** (تماس، هدیه‌ها، کتاب‌خوانی مشترک، خونه‌ی رویایی، پل زبان) به‌همراه
  **جستجوی سراسری** — یعنی شش بخش تازه در مجموع.
* **۵ مدل جدید** در پنج اپ تازه + `LiveLocation` در `accounts` + دو کلید تنظیمات جستجو در `UserConfig`.
* **۲۱ منبع جستجو** با غلط‌گیری فارسی، فیلتر اپ/تاریخ/نوع و کش ۴۵ ثانیه‌ای.
* **۱۶ نشان و ۶ فصل آموزش تازه** برای اپ‌های جدید.
* **یادآوری تماس** در `sweep` با پنجره‌ی قابل تنظیم (۵ تا ۱۸۰ دقیقه).
* **ضبط صدا با میکروفن** در گزارش تماس و تلفظ کلمه‌ها (بدون نیاز به آپلود دستی).
* **۹۷ تست بک‌اند** به‌علاوه‌ی ۹ سوئیتِ فرانت‌اند (`npm run test:ui`، ۲۸۵ بررسی) برای مدیر پنجره، تنظیمات
  (زبان/تم)، جستجوی سراسری، رندر همه‌ی اپ‌های تازه با پاسخ‌های واقعی بک‌اند، و لایه‌های عمقِ
  سه‌بعدی در هر سه لایه‌ی کیفیت.

---

---

## ۱۳. لایه‌ی کیفیتِ سه‌بعدی

پوسته‌ی سه‌بعدیِ LoveOS چهار لایه دارد. اصلِ حاکم این است: **کیفیت هرگز
نباید به قیمتِ «کار نکردن» تمام شود.** اگر دستگاه نتواند، سیستم خودش یک
پله پایین می‌آید و اپ سرِ پایش می‌ماند.

### لایه‌ها

| مقدارِ ذخیره‌شده | نامِ فارسی | چه چیزی فعال است |
|---|---|---|
| `auto` | خودکار | سنجه‌ی توانِ دستگاه + سنجشِ فریمِ واقعی، بعد انتخابِ خودکار |
| `lite` | مهتاب | عمقِ «نقاشی‌شده»: سایه، پخ، گرادیانِ سطح — بدونِ `transform` سه‌بعدی |
| `balanced` | بلور | چرخشِ CSS، تیلت با نشانگر (دسکتاپ) و ژیروسکوپ (اندروید)، برجسته‌سازیِ لایه‌ای |
| `dream` | کهکشان | همه‌ی بلور، به‌علاوه‌ی WebGL واقعی: آسمانِ ستاره‌ها با `three.js` و کره‌ی زمین روی نقشه |

نام‌های فارسی عمداً شاعرانه‌اند چون این تنظیمات را یک کودک می‌خواند؛ ولی
**مقدارِ ذخیره‌شده** همان `lite|balanced|dream` می‌ماند تا منطقِ موتور و
بک‌اند به زبانِ نمایش گره نخورد.

### کجا چیست

| فایل | نقش |
|---|---|
| `frontend/src/shared/quality.ts` | سنجه‌ی توانِ دستگاه، امتیازدهی، نگهبانِ فریم، `attachPointerTilt` / `attachGyroTilt`، `canUseGlobe` |
| `frontend/src/shared/depth.tsx` | جعبه‌ابزارِ مشترک: `useQualityTier`، `useMotionAllowed`، `useDepthFactor`، `useCardTilt`، `Tilt`، `Extrude`، `Specular`، `AmbientDepth`، `DepthHero` |
| `frontend/src/styles/index.css` | توکن‌های CSS: `--q3d`، `--rim-light`، `--ao-shadow`، `--well-face`، `--btn-edge`، `--light-x/y` |
| `frontend/src/three/useThreeScene.ts` | تنها نقطه‌ی ساختِ `WebGLRenderer` در کلِ پروژه + سقفِ صحنه‌های زنده + آزادسازیِ کامل |
| `frontend/src/three/scenes/starmap.ts` | صحنه‌ی آسمانِ ستاره‌ها |
| `frontend/src/three/scenes/cinema.ts` + `CinemaHall.tsx` | صحنه‌ی سالنِ سینما + پوسته‌ی `lazy` با تنزلِ CSS |
| `frontend/src/three/scenes/room.ts` + `DreamRoom.tsx` | ماکتِ سه‌بعدیِ خانه‌ی رؤیایی + پوسته‌ی `lazy` با تنزلِ CSS |
| `frontend/tests/three-hygiene.ts` | ۲۹ بررسیِ ایستا روی متنِ منبع برای قاعده‌هایی که jsdom هرگز نمی‌بیندشان |
| `backend/content/models.py` | `Constellation.kind` (حرف یا شکل) |
| `backend/accounts/models.py` | `UserConfig.ui_quality` |

### چرا بیشترِ کار در CSS است نه JS

متغیرِ `--q3d` در مهتاب `0` و در بلور/کهکشان `1` است. مقادیرِ سه‌بعدی در
آن **ضرب** می‌شوند:

```css
.os-stage-3d { perspective: calc(1200px * var(--q3d) + 100000px * (1 - var(--q3d))); }
.os-depth    { transform: rotateY(calc(var(--tilt-x, 0deg) * var(--q3d))); }
.os-sky-depth-far { transform: translateZ(calc(-90px * var(--q3d))); }
```

نتیجه: در مهتاب پرسپکتیو عملاً بی‌نهایت و چرخش صفر می‌شود، **بدونِ یک
شاخه‌ی `if` در جاوااسکریپت** و بدونِ رندرِ دوباره‌ی React هنگامِ تغییرِ تم.
این هم باندل را کوچک نگه می‌دارد هم ریسکِ «یادمان رفت فلان اپ را گیت
کنیم» را از بین می‌برد.

### قانون‌های سختی که نباید شکسته شوند

۱. **متن هرگز کج نمی‌شود.** `Tilt` فقط دورِ عناصرِ بصری (قلب، گیاه، عکسِ
   خاطره، جعبه‌ی هدیه) و هرگز دورِ ظرفِ متن یا ناحیه‌ی اسکرول.
۲. **`Tilt`/`Extrude` داخلِ ناحیه‌ی اسکرول باشند، نه خودِ ظرفِ اسکرول.**
   سافاری وقتی `overflow` چیزی غیرِ `visible` باشد، `preserve-3d` را تخت
   می‌کند و کلِ اثر بی‌صدا می‌میرد.
۳. **دو منبعِ transform روی یک عنصر نگذارید.** انیمیشنِ CSS در آبشار بر
   `style` درون‌خطی اولویت دارد، پس انیمیشنِ framer یا متغیرِ تیلت یکی
   دیگری را بی‌صدا می‌بلعد. هر کدام عنصرِ خودش را می‌خواهد.
۴. **`color-mix()` ممنوع.** سافاری ۱۶٫۲+ می‌خواهد و گوشیِ سه-چهار ساله‌ی
   هدفِ ما iOS 15 دارد. به‌جایش `linear-gradient` و `rgba`.
۵. **`Extrude` را به کپی‌کردنِ `children` برنگردانید.** اگر `children` یک
   `<defs>` با `id` داشته باشد، N کپی یعنی N `id` تکراری → HTML نامعتبر و
   همه‌ی لایه‌ها نخستین گرادیان را برمی‌دارند. `Extrude` به‌جایش یک
   `path` می‌گیرد و `defs` فقط یک بار در لایه‌ی جلو رندر می‌شود. سوئیتِ
   `depth-tiers` یگانگیِ `id="hb"` را قفل کرده است.
۶. **هر صحنه‌ی `three.js` باید آزاد شود.** `useThreeScene` سقفِ سراسریِ
   ۲ صحنه‌ی زنده دارد (iOS Safari سخت‌گیر است و قدیمی‌ترین `context` را
   بی‌صدا می‌کُشد)، و در پاک‌سازی `geometry`/`material`/`texture` را جداگانه
   `dispose` و `forceContextLoss` را صدا می‌کند.

### نگهبانِ فریم

در لایه‌ی کهکشان، یک نگهبان فریم را در پنجره‌های ۲٫۲ ثانیه‌ای می‌سنجد. اگر
دو بارِ پیاپی زیرِ آستانه باشد، یک پله تنزل می‌دهد، آن لایه را در
`localStorage` به خاطر می‌سپارد (کلیدِ `loveos_quality_downgrade_v1`) تا نشستِ
بعد همان کندی تکرار نشود، و یک پیامِ ملایم و **یک‌بار** نشان می‌دهد. بی‌صدا
تنزل نمی‌دهد چون کودک فکر می‌کند اپ خراب شده است.

نگهبان وقتی صفحه پنهان است مکث می‌کند؛ وگرنه یک تبِ پس‌زمینه فریمِ پایین
گزارش می‌کرد و کیفیت بی‌دلیل کم می‌شد.

### سه تله‌ی واقعی که در کد گارد دارند

* **سنجشِ فریم حینِ بوت** عددِ غلطِ پایین می‌دهد. موتور ابتدا با امتیازِ
  ایستا تصمیم می‌گیرد، بعد سنجشِ فریم را به یک لحظه‌ی بیکارِ مرورگر
  (`requestIdleCallback`) موکول می‌کند.
* **ژیروسکوپ در iOS** نیاز به پنجره‌ی اجازه دارد؛ برای یک افکتِ تزئینی
  قابلِ توجیه نیست، پس `attachGyroTilt` روی iOS `null` برمی‌گرداند.
* **`pointermove` روی لمس** حینِ اسکرول هم شلیک می‌شود؛ اگر گوش می‌دادیم،
  کارت‌ها موقعِ اسکرول کج می‌شدند (رابطِ دریازده). پس تیلتِ نشانگر فقط
  `pointerType === 'mouse'` را می‌پذیرد.

### کره‌ی زمین روی نقشه

`canUseGlobe(tier, report)` یک تابعِ **خالص** در `shared/quality.ts` است تا
بدونِ بالا‌آوردنِ maplibre آزموده شود. سه شرط: فقط `dream`، هرگز روی GPU
خانواده‌ی **Mali** (باگِ شناخته‌شده‌ی دقتِ عرضِ جغرافیاییِ maplibre در
projection کره‌ای، issue #7419؛ تهران و استانبول نزدیکِ نوارِ خطرند)، و فقط
WebGL واقعیِ غیرِنرم‌افزاری. این گارد **فقط کره** را خاموش می‌کند نه کلِ
لایه‌ی کیفیت را. `setProjection` هم داخلِ `try/catch` است تا اگر روی driver
خاصی انداخت، نقشه سفید نشود.

### باندل و PWA

`three.js` (۱۳۵KB gzip) پشتِ `lazy()` در ماژولِ جداست و از `precache` هم
مستثنا شده (`globIgnores` در `vite.config.ts`) و به‌جایش `runtimeCaching` از
نوعِ `CacheFirst` گرفته. یعنی کاربرِ مهتاب هرگز دانلودش نمی‌کند، ولی اولین
بار که کسی واقعاً آسمانِ سه‌بعدی را باز کند کش می‌شود و بعدش آفلاین هم کار
می‌کند.

⚠️ **هر** پوسته‌ی `lazy` در `frontend/src/three/*.tsx` — `StarmapSky`،
`CinemaHall`، `DreamRoom` و هر صحنه‌ی چهارمی که بعداً اضافه شود — باید هم در
`globIgnores` باشد هم در `urlPattern`ِ نوعِ `CacheFirst` در `vite.config.ts`؛
وگرنه چانکش بی‌صدا به `precache` برمی‌گردد و کاربرِ مهتاب هم دانلودش می‌کند.

این قبلاً فقط یک هشدارِ کامنتی بود و دقیقاً به همین دلیل یک بار `CinemaHall`
به `precache` نشت کرد. حالا **ماشینی سنجه می‌شود**: `tests/three-hygiene.ts`
محتوایِ واقعیِ پوشه‌ی `src/three/` را می‌خواند و با هر دو فهرست مقایسه
می‌کند، پس صحنه‌ی فراموش‌شده به‌جای رسیدن به دستِ کاربر، build را می‌شکند.

### افزودنِ سه‌بعد به یک اپِ تازه

```tsx
import { Tilt, Extrude, useMotionAllowed, useQualityTier } from '../shared/depth'

const tier = useQualityTier()          // ⚠️ هوک‌ها بالایِ هر returnِ زودهنگام
const allowed = useMotionAllowed()     // ⚠️ هر دو بدونِ شرط؛ && بینِ دو هوک
const deep = tier !== 'lite' && allowed //    نقضِ Rules-of-Hooks است

// عنصرِ بصری (نه متن!) را بپیچید:
{deep ? <Tilt maxDeg={9}>{art}</Tilt> : art}
```

برای یک جسمِ توپُر از `Extrude` استفاده کنید (یک `path`، نه `children`)، و
برای حرکتِ پس‌زمینه از `AmbientDepth` — خودش در مهتاب صفر و در بلور نصفِ
ذره‌ها را می‌سازد.

### تست

دو سوئیت، چون دو جور باگ داریم:

**`frontend/tests/depth-tiers.tsx`** (۷۱ بررسیِ رندری) هر سه لایه را با
`useOS.setState({ uiQuality })` اجبار می‌کند (موتور در محیطِ آزمون به مهتاب
وتو می‌کند، پس راهِ دیگری برای رسیدن به شاخه‌های عمیق نیست) و این‌ها را
می‌سنجد: یگانگیِ `id="hb"`، بیرون بودنِ متن‌ها از ظرفِ تیلت، تعدادِ لایه‌های
برجسته‌سازی در هر لایه، تنزلِ باوقار هنگامِ نبودِ WebGL، استنتاجِ `kind` از
`letter` وقتی API آن را نمی‌فرستد، هدفِ لمسیِ ≥۴۴ پیکسل، هفت حالتِ
`canUseGlobe`، و اپ‌های تازه‌ی سه‌بعدی‌شده (نشان‌ها، حالِ دل، سینما،
ترمینال) در **هر سه لایه** — از جمله اینکه سینما در نبودِ WebGL سالنِ CSS
نشان دهد نه بومِ سیاه، و اینکه شیشه‌ی CRT روی ظرفِ اسکرول نباشد.

**`frontend/tests/three-hygiene.ts`** (۲۹ بررسیِ ایستا) روی **متنِ منبع**
کار می‌کند، بدونِ jsdom و بدونِ React، زیرِ یک ثانیه. دلیلِ وجودش این است که
بیشترِ باگ‌های این فاز رندری نبودند: یا فقط در مرورگرِ واقعی دیده می‌شوند
(سافاری `preserve-3d` را در ظرفِ `overflow-hidden` تخت می‌کند)، یا فقط موقعِ
build (چانکِ `three.js` بی‌سروصدا به `precache` سرِ کار می‌رود)، یا فقط روی
دستگاهِ واقعی (افتِ فریم). هر بررسیِ بخش‌های ۱ تا ۵ یک باگِ **واقعاً
رخ‌داده** در همین پروژه است، نه یک قاعده‌ی حدسی. بخشِ ۶ جنسش فرق می‌کند:
**گاردِ پوشش** است. وعده‌ی این فاز «کلِ پروژه سه‌بعدی شد» بود و تا وقتی با
یک حلقه‌ی دستیِ shell سنجیده می‌شد قابلِ اتکا نبود؛ حالا هر اپِ
`src/apps/*.tsx` باید دستِ‌کم یک نشانگرِ عمق داشته باشد و اپِ سی‌ویکمِ
تخت، build را می‌شکند. تنها استثنا صریح و دلیل‌دار است (`DreamHome`، که
عمقش را به ماکتِ سه‌بعدیِ `three/DreamRoom.tsx` واگذار می‌کند) و آزمون
**مقصد را هم می‌سنجد** تا همان فهرستِ استثنا راهِ فرارِ هر اپِ تنبلی نشود.

⚠️ یک شکستِ گهگاهی هم در همین فاز پیدا و ریشه‌یابی شد. fixtureِ
`window-manager-settings` رویدادها را با `new MouseEvent(...)` می‌فرستاد و
jsdom روی `MouseEvent` هیچ `pointerType` نمی‌گذارد. شرطِ دسکتاپ
`e.pointerType !== 'mouse'` است، پس `undefined !== 'mouse'` → کلیکِ موس
**لمس** خوانده می‌شد و یک تایمرِ ۳۲۰ میلی‌ثانیه‌ای (`ARM_DELAY_MS`) برای
آماده‌سازیِ درگ مسلح می‌شد که ordinarily در `pointerup` پاک می‌شود. زیرِ
بارِ سنگینِ CPU آن فاصله از ۳۲۰ میلی‌ثانیه بیشتر شد، تایمر شلیک شد،
`suppressClickRef` ست شد و کلیک **قورت داده شد** — پنجره‌ی دوم باز نشد و
بررسیِ «دو پنجره باز است» شکست. حالا fixture صریحاً `pointerType: 'mouse'`
می‌دهد و رقابتِ زمانی از ریشه حذف شده. (در شش اجرایِ بعدی بدونِ اصلاح هم
تکرار نشد، پس این ریشه‌یابی از راهِ خواندنِ کد تأیید شد نه بازتولید؛ ولی
مسیرِ مسلح‌شدنِ تایمر در کد هست و حذفش درست است.)

⚠️ و یک تله‌ی کوچک‌تر در همان اصلاح: `button` روی `MouseEvent` **فقط
getter** دارد و `Object.assign(e, { button: 0 })` با `TypeError` می‌ترکد.
`desktop-drag.tsx` می‌تواند `button` بدهد چون رویدادش را از `Event`ِ خام
می‌سازد، نه `MouseEvent`.

⚠️ نسخه‌ی اولِ این سوئیت شش شکست گزارش کرد که **هر شش مثبتِ کاذب** بودند:
متنِ خودِ کامنت‌ها با الگوها می‌خواند. مثلاً در `Vault.tsx` کامنتی هست که
توضیح می‌دهد چرا `useQualityTier() !== 'lite' && useMotionAllowed()` غلط است
— و آزمون همان کامنت را گناهکار گرفت. حالا `stripComments()` محتوای
کامنت‌ها را با فاصله جایگزین می‌کند و **شماره‌ی خط‌ها را نگه می‌دارد** تا
گزارش به خطِ درستِ کد اشاره کند. و `//` فقط در JS/TS کامنت است نه در CSS،
وگرنه یک `url(https://…)` بقیه‌ی خط را پاک می‌کرد.

⚠️ این سوئیت `process.exit` صریح می‌گیرد: زنجیره‌ی `setTimeout` ضربان و
انیمیشن‌های بی‌پایانِ framer رویداد-loopِ Node را زنده نگه می‌دارند و بدونِ
آن، آزمون پاس می‌شود ولی پروسه تا `timeout` آویزان می‌ماند.

## ۱۴. سه‌بعد در همه‌ی اپ‌ها

بخشِ ۱۳ **موتور** را توضیح می‌دهد؛ این بخش می‌گوید آن موتور روی **هر ۳۰ اپ**
چطور نشسته است. اصلِ راهنما این بود که سه‌بعد یک **زبانِ طراحی** است نه یک
جلوه‌ی تزیینی: هر اپ باید وزن و عمق داشته باشد، بدونِ اینکه خواندنِ محتوا
سخت‌تر شود.

### سه دسته‌ی برخورد

اپ‌ها یکسان نیستند، پس سه‌بعدی‌شدنشان هم یکسان نیست. این دسته‌بندی عمداً
پیش از نوشتنِ هر خط کد ثابت شد:

| دسته | اپ‌ها | چه کار می‌کند |
|---|---|---|
| **لحظه‌ی امضا** | ضربانِ دل، آغوش، باغ، حالِ دل، گنجه‌ی صدا، نشان‌ها، چت، موزیک، سینما، پازل، خانه‌ی رؤیا، نقشه‌ی ما | یک جسمِ سه‌بعدیِ اختصاصی که هویتِ همان اپ است: قلبِ چندلایه، نوارِ کاست با قرصِ چرخان، مدالِ فلزی، صفحه‌ی گرامافون، سالنِ سینما، ماکتِ خانه، کره‌ی زمین |
| **عمقِ خویشتن‌دار** | تماس، چرخه، پلِ زبان، کتاب‌خوانی، آزمون، نقشه‌ی آینده، راهنما، تنظیمات، درباره‌ی ما | عمقِ نقاشی‌شده + یک نقطه‌ی کانونی + پارالاکسِ سرصفحه. **سطرهای داده، جدول، تقویم و فرم هرگز کج نمی‌شوند** و منطقِ اپ دست‌نخورده می‌ماند |
| **استثنای عمدی** | ترمینال | شیشه‌ی CRT با انحنای گوشه و فسفرِ درخشان — استثنا از زبانِ رزین/بلور، چون خودِ ترمینال یک نمایشگرِ قدیمی است و تقلید از جنسِ خودش صادقانه‌تر است |

**چهار روکشِ خودِ سیستم** هم ورق می‌خورند: منوی استارت، مرکزِ اعلان و روکشِ
راز از پایین باز می‌شوند (`rotateX` منفی) و جست‌وجوی سراسری از بالا (مثبت) —
جهتِ چرخش با جهتِ ورود یکی است، وگرنه ورق‌خوردن خلافِ فیزیک به نظر می‌رسد.

### جعبه‌ابزارِ مشترک

بیشترِ اپ‌ها با **کلاسِ CSS** سه‌بعدی می‌شوند نه با کامپوننت. این عمداً است:
کلاس هیچ لیسنری اضافه نمی‌کند، هیچ رندرِ تازه‌ای نمی‌سازد، و مهم‌تر از همه
**نمی‌تواند منطقِ اپ را بشکند**.

| ابزار | چه می‌کند | کی استفاده شود |
|---|---|---|
| `.os-tilt-card` / `-medal` / `-tile` | تیلت با نشانگر یا ژیروسکوپ (۴°/۱۵°/۹°) — همه با **یک** لیسنرِ واگذارشده روی `document` | کارت یا جسمِ بصریِ کوتاه. هرگز روی متن |
| `.os-depth-list` | فرزندانِ فهرست را با تأخیرِ پلکانی در محورِ Z وارد می‌کند | وقتی فرزندانِ فهرست `motion.*` **نباشند** |
| `.os-slab` | عمقِ نقاشی‌شده: پخ + سایه + گرادیان، بدونِ هیچ چرخشی | سطری که متن یا عدد دارد و باید تخت بماند |
| `.os-orb` / `.os-disc` | هاله‌ی نورانی و قرصِ فلزی پشتِ یک عنصر | لحظه‌ی کانونیِ یک اپ |
| `.os-crt` / `.os-crt-glow` | شیشه‌ی CRT و فسفرِ متن | فقط ترمینال |
| `.os-parallax-far/mid/near` | سه سرعتِ پارالاکس | سرصفحه‌ها |
| `.os-cinema-*` / `.os-house-*` | تنزلِ CSSِ دو صحنه‌ی WebGL | وقتی صحنه در دسترس نیست |
| `useDepthFactor()` | `0` در مهتاب و کم‌حرکتی، `1` در بقیه | وقتی مقدارِ چرخش را در framer می‌نویسی |
| `DepthHero` | سرصفحه‌ی آماده با پارالاکس و عمق | سرِ اپ‌ها |

همه‌ی این‌ها در `--q3d` ضرب می‌شوند، پس در مهتاب خودبه‌خود بی‌اثرند — بدونِ
یک `if` در جاوااسکریپت.

### دو صحنه‌ی WebGLِ تازه و بودجه‌ی صحنه‌ها

`useThreeScene` یک سقفِ سخت دارد: **`MAX_LIVE_SCENES = 2`**. حالا سه صحنه‌ی
بالقوه وجود دارد (آسمانِ ستاره‌ها، سالنِ سینما، ماکتِ خانه)، پس صحنه‌ی سومِ
هم‌زمان `onUnavailable('budget')` می‌گیرد و اپ به تنزلِ CSS می‌رود.

چرا ۲ و نه ۳: هر `WebGLRenderer` زنده یک context جدا روی GPU است با حافظه و
یک لایه‌ی compositorِ خودش. سقفِ مرورگر حدودِ ۱۶ تا است، ولی روی یک گوشیِ
میان‌رده صداقت یعنی دو. خطرِ واقعی هم همین است: کاربر سینما را باز کند و
پیش از تمام‌شدنِ `dispose` بپرد روی خانه‌ی رؤیا.

هر دو صحنه **پوسته‌ی تنزلِ CSS** دارند و مسیرشان در آزمون سنجیده می‌شود:

| صحنه | تنزل |
|---|---|
| آسمانِ ستاره‌ها | همان SVGِ مرجعِ پیشین (بدونِ تغییر) |
| `CinemaHall` | `.os-cinema-hall` — سالنِ CSS-سه‌بعدی با سه ردیفِ صندلی، پرده و نور |
| `DreamRoom` | `.os-house-*` — ماکتِ CSSِ خانه با سه ظرفِ جدا |

### راهنمای تصمیم برای هر اپ

دو حالت دارد و انتخابشان یک قاعده‌ی ساده است:

- فرزندانِ ظرف **transformِ inline ندارند** → کلاسِ CSS (`.os-depth-list`،
  `.os-slab`، `.os-tilt-*`). ریسکِ صفر، لیسنرِ صفر، منطقِ دست‌نخورده.
- فرزندان **`motion.*` هستند** → همان انیمیشنِ موجود را با `rotateX`/`z`
  ضرب‌شده در `dz` ارتقا بده و `transformPerspective` را در **`style`** بگذار
  (در `transition` = خطای TS2559). کلاسِ `.os-depth-list` این‌جا با
  transformِ inline می‌جنگد و باید کنار گذاشته شود.

### تله‌هایی که در این فاز واقعاً خوردیم

این‌ها حدس نیستند؛ هرکدام یک بار کد را شکست و اصلاحش در کد مانده است:

| تله | چرا خطرناک است | راهِ درست |
|---|---|---|
| `rotateY: 90` بدونِ پرسپکتیو | `cos(90°) = 0` یعنی عنصر **له** می‌شود، نه اینکه بچرخد — کارتِ فلشِ پلِ زبان دقیقاً همین باگ را داشت | همیشه `transformPerspective` |
| شیشه روی ظرفِ اسکرول | `::after` موقعیتِ `absolute` دارد و با محتوا اسکرول می‌شود، پس بازتاب روی متن می‌لغزد | `.os-crt` روی پوسته‌ی بیرونیِ بدونِ اسکرول؛ خودِ ظرفِ اسکرول شفاف |
| انیمیشنِ CSS روی transformِ inline | `animation` روی inline transform **می‌چربد**؛ ایموجیِ `animate-float` هرگز `translateZ` نمی‌گیرد | انیمیشن بیرون، transformِ framer درون — یا یک عنصرِ جدا برای هاله |
| کلیدِ سه‌بعدیِ فراموش‌شده در `animate` | اگر `rotateX` فقط در `initial` باشد، عنصر برای همیشه کج می‌ماند — باگِ واقعیِ روکشِ راز | کلیدِ سه‌بعدی در **هر سه** شاخه‌ی `initial`/`animate`/`exit` |
| سافاری و `overflow-hidden` | `overflow-hidden` روی والد، `preserve-3d` را **تخت** می‌کند | برای فهرستِ اسکرول‌شدنی از تابعِ خودبسنده‌ی `perspective()` داخلِ transform استفاده شود، نه از `perspective` روی والد |
| `manualChunks` و `/src/three/` | اگر پوشه‌ی `src/three` به `manualChunks` برود، `quality.ts` هم مهاجرت می‌کند و `index.js` **ایستا** به `three` وابسته می‌شود — یعنی کاربرِ مهتاب هم ۵۵۰ کیلوبایت می‌گیرد | `manualChunks` فقط `node_modules/three` |
| `animation-fill-mode: both` | آخرین فریم برای همیشه می‌ماند و فهرست در Zِ انتهایی گیر می‌کند | `backwards` |

### باندل بعد از این فاز

| چانک | اندازه | gzip | precache؟ |
|---|---|---|---|
| `index.js` (اصلی) | ۳۲۸٫۵ کیلوبایت | ۱۰۲٫۱ | ✅ |
| `index.css` | ۵۷٫۶ کیلوبایت | ۱۲٫۴ | ✅ |
| `loveos-3d` (three.js) | ۵۵۱٫۷ کیلوبایت | ۱۳۶٫۸ | ❌ — `CacheFirst` |
| `CinemaHall` | ۵٫۳ کیلوبایت | ۲٫۵ | ❌ |
| `DreamRoom` | ۵٫۳ کیلوبایت | ۲٫۴ | ❌ |

سه صحنه‌ی WebGL با هم **۵۶۲ کیلوبایتِ خام** هستند و کاربرِ مهتاب هیچ‌کدام را
دانلود نمی‌کند. دو پوسته‌ی تازه فقط ۱۰٫۶ کیلوبایت اضافه کردند چون خودِ
three.js در `loveos-3d` مشترک است.

<div align="center">

**LoveOS نسخه‌ی ۲.۰**

*ساخته‌ی بابا، برای دخترش.*
*فاصله فقط یه عدده. قلبمون همیشه یکیه.* ❤

</div>

</div>
