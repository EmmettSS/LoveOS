<div dir="rtl">

# LoveOS — مستندات فنی و راهنمای کاربری

> یه سیستم‌عامل کوچولوی عاشقانه توی مرورگر.
> ساخته‌ی بابا، برای دخترش، از آن‌سوی فاصله‌ای که فقط یه عدده.

**نسخه:** ۱.۰ · **زبان پیش‌فرض:** فارسی · **زبان دوم:** انگلیسی
**نسخه‌ی انگلیسی این مستند:** [`DOCUMENTATION.md`](./DOCUMENTATION.md)

---

## فهرست

۱. [معرفی کلی](#۱-معرفی-کلی)
۲. [معماری](#۲-معماری)
۳. [نصب و راه‌اندازی](#۳-نصب-و-راهاندازی)
۴. [بیست‌وهفت اپ](#۴-بیستوهفت-اپ)
۵. [پنل بابا](#۵-پنل-بابا)
۶. [اتصال به بات سروش](#۶-اتصال-به-بات-سروش)
۷. [مرجع API](#۷-مرجع-api)
۸. [رازها](#۸-رازها)
۹. [استقرار روی سرور](#۹-استقرار-روی-سرور)
۱۰. [نگهداری و رفع اشکال](#۱۰-نگهداری-و-رفع-اشکال)

---

## ۱. معرفی کلی

### LoveOS چیست

LoveOS یک **اپ وب پیش‌رونده (PWA)** است که خودش را شبیه یک سیستم‌عامل کوچک نشان می‌دهد: بوت می‌شود،
رمز می‌پرسد، دسکتاپی با ویجت و داک نشان می‌دهد و ۲۷ اپ کوچک را در خودش جا داده — هر کدام یک راه
متفاوت برای گفتن «دوستت دارم» از آن‌سوی یک فاصله‌ی طولانی.

این پروژه هدیه‌ی یک پدرِ برنامه‌نویس («بابا») به دخترش/نامزدش («دخترم») است. لحن همه‌جا گرم،
بچگانه و بامزه است و همه‌ی متن‌ها مستقیم با خود او حرف می‌زنند.

### اصول طراحی

| اصل | یعنی چه |
|---|---|
| **هیچ‌چیز هاردکد نیست** | تمام محتوا — پیام‌ها، ویس‌ها، نامه‌ها، عکس‌ها، سؤال‌های کوییز، جواب‌های ترمینال، حتی متن رازها — در پنل جنگو است و بدون دست زدن به کد عوض می‌شود. |
| **مخفی بودن از پایه** | اپ روی یک دامنه‌ی اعلام‌نشده است که فقط با QR پیدا می‌شود. `robots.txt` همه‌چیز را می‌بندد، همه‌ی پاسخ‌ها هدر `X-Robots-Tag: noindex` دارند و پنل پشت یک مسیر مخفی و یک رمز جداگانه است. |
| **اول موبایل، بعد دسکتاپ** | برای گوشیِ توی دست او طراحی شده؛ روی صفحه‌ی بزرگ اپ‌ها به پنجره‌های شناور و قابل جابه‌جایی تبدیل می‌شوند. |
| **دو زبان، صفر متن هاردکد** | همه‌ی رشته‌ها در `frontend/public/locales/{fa,en}/translation.json` هستند. فارسی پیش‌فرض است و چیدمان راست‌به‌چپ. |
| **کار کردن بدون اینترنت** | سرویس‌ورکر پوسته، فونت‌ها و آیکن‌ها را کش می‌کند تا حتی با اینترنت بد هم باز شود. |
| **مهربان بودن به‌صورت پیش‌فرض** | صداها همه سنتز می‌شوند (بدون فایل کپی‌رایتی)، انیمیشن‌ها به `prefers-reduced-motion` احترام می‌گذارند و اپ حساس سلامت پیام سلب مسئولیت پزشکی دارد. |

### مسیر او در اپ

```
QR  →  دامنه‌ی مخفی
          │
          ▼
    ┌──────────┐   خط‌های بوت تایپ می‌شوند، ملودی نرم، «سلام دخترم...»
    │   بوت    │
    └────┬─────┘
         ▼
    ┌──────────┐   «امروز N روزه که بابا عاشقته.»
    │   قفل    │   رمز · سؤال امنیتی · بعد از ۵ اشتباه: «کمک از بابا»
    └────┬─────┘
         ▼
    ┌──────────┐   ساعت · شمارش معکوس · هوای دو شهر · پیام امروز
    │  دسکتاپ  │   ۲۴ آیکن · داک · منوی شروع · مرکز اعلان
    └────┬─────┘
         ▼
      ۲۷ اپ
```

### ساختار پروژه

```
LoveOS/
├── backend/                 جنگو ۵ + DRF
│   ├── config/              تنظیمات و مسیرهای اصلی
│   ├── core/                اعلان، صف سروش، دستاورد، شمارنده،
│   │                        رازها، کلاینت سروش، دستور sweep
│   ├── accounts/            UserConfig (منبع یگانه‌ی حقیقت)، نشست‌ها
│   ├── content/             ویس، آهنگ، خاطره، نامه، شمارش معکوس،
│   │                        باغچه، ستاره‌ها، سینما، کوییز، آرزوها،
│   │                        حال دل، صندوقچه، آموزش، دستورهای ترمینال
│   ├── social/              چت، بغل، یادآورهای مهربان
│   ├── health/              چرخه، علائم، داروها، مراقبت از خود
│   ├── library/             کتاب، فصل، صفحه، پاراگراف، حاشیه‌نویسی
│   ├── games/               پازل قلب
│   ├── templates/           صفحه‌ی دروازه‌ی پنل
│   └── media/               فایل‌های آپلودی
├── frontend/                ری‌اکت ۱۹ + ویت + تایپ‌اسکریپت
│   ├── public/
│   │   ├── locales/         فایل‌های ترجمه‌ی fa و en
│   │   ├── fonts/           فونت‌های میزبانی‌شده‌ی محلی (بدون CDN)
│   │   ├── icons/           لوگو و آیکن‌های PWA
│   │   └── backgrounds/     پس‌زمینه‌ی بوت / قفل / دسکتاپ
│   └── src/
│       ├── os/              بوت، قفل، دسکتاپ، داک، منوی شروع،
│       │                    مرکز اعلان، پنجره، پرده‌ی رازها
│       ├── apps/            ۲۵ کامپوننت اپ
│       ├── shared/          api، store، i18n، format، sound، Icon، ui
│       ├── App.tsx          ماشین مرحله‌ها + شنونده‌های سراسری رازها
│       └── main.tsx
├── scripts/                 dev.sh · build.sh · deploy.sh · qr.py
├── DOCUMENTATION.md         نسخه‌ی انگلیسی
└── DOCUMENTATION_FA.md      همین فایل
```

---

## ۲. معماری

### پشته‌ی فناوری

**بک‌اند**

| بخش | انتخاب | چرا |
|---|---|---|
| فریم‌ورک | Django 5.2 | پنل ادمین رایگانش، **خودِ پنل بابا** است. |
| API | Django REST Framework | ویوهای ساده‌ی تابعی، یک فایل برای هر حوزه. |
| دیتابیس | SQLite (توسعه) / MySQL (پروداکشن) | با `DB_ENGINE` سوییچ می‌شود؛ MySQL با `utf8mb4`. |
| احراز هویت | توکن سفارشی | فقط یک کاربر داریم؛ سیستم کاربر جنگو برای پنل نگه داشته شده. |
| زمان‌بندی | `manage.py sweep` با cron | یک دستور در دقیقه؛ اگر ردیس بود، Celery هم اختیاری است. |
| اعلان | بات سروش‌پلاس | API سازگار با تلگرام روی `api.splus.ir`. |

**فرانت‌اند**

| بخش | انتخاب |
|---|---|
| فریم‌ورک | React 19 + TypeScript + Vite 8 |
| استایل | Tailwind CSS 3 با متغیرهای CSS برای تم روز/شب |
| انیمیشن | Framer Motion (و GSAP در دسترس) |
| وضعیت | Zustand (`useOS`) |
| چندزبانگی | i18next + react-i18next |
| نقشه | MapLibre GL + کاشی‌های OpenStreetMap (بدون کلید API) |
| صدا | Web Audio API (سنتزی) + `<audio>` برای فایل‌های آپلودی |
| تاریخ | jalaali-js برای تقویم شمسی |
| PWA | vite-plugin-pwa (Workbox، به‌روزرسانی خودکار) |

> **درباره‌ی سه‌بعدی:** `@react-three/fiber` به React نسخه‌ی `>=19 <19.3` نیاز داشت که با نسخه‌ی
> قفل‌شده‌ی ما تضاد داشت، پس حذف شد. آسمان ستاره‌ها با SVG متحرک رسم می‌شود — سبک‌تر، واضح‌تر و
> روی گوشی‌های قدیمی هم روان.

### مسیر یک درخواست

```
  مرورگر (PWA)
      │  فقط آدرس نسبی: /api/... و /media/...
      ▼
  Nginx  ──/api/──►  Gunicorn ──► Django
      │              /daddy-panel-9x7k/
      ├──/static/──► فایل‌های استاتیک
      ├──/media/───► آپلودها
      └── /  ──────► frontend/dist (بازگشت به index.html)
```

در حالت توسعه، ویت مسیرهای `/api`، `/media`، `/static`، `/healthz` و مسیر پنل را به
`127.0.0.1:8000` پراکسی می‌کند تا مرورگر فقط با یک دامنه حرف بزند.

### مدل احراز هویت

دقیقاً **یک** دختر و **یک** بابا داریم، پس برای خود اپ جدول کاربر وجود ندارد.

۱. `POST /api/auth/unlock {passcode}` رمز را با هش نمک‌دار در `UserConfig` مقایسه می‌کند.
۲. در صورت درست بودن یک `DeviceSession` ساخته و یک توکن تصادفی برگردانده می‌شود.
۳. فرانت آن را در `localStorage` با کلید `loveos_token` نگه می‌دارد و در هر درخواست
   `Authorization: Token <token>` می‌فرستد.
۴. نشست‌ها بعد از `SESSION_TTL_HOURS` (پیش‌فرض ۳۰ روز) منقضی می‌شوند.
۵. هر پاسخ `401` توکن را پاک می‌کند و رابط کاربری به صفحه‌ی قفل برمی‌گردد.
۶. صندوقچه رمز **دوم** دارد که فقط `VAULT_SESSION_MINUTES` (پیش‌فرض ۲۰ دقیقه) باز می‌ماند.

تلاش‌های ناموفق در `UnlockAttempt` ثبت می‌شوند. بعد از `MAX_UNLOCK_ATTEMPTS` (۵ بار) دکمه‌ی
«کمک از بابا» ظاهر می‌شود که به سروش بابا پیام می‌دهد — و بی‌سروصدا یک راز را هم فعال می‌کند.

### ماشین مرحله‌ها

`useOS.phase` یکی از `boot | lock | desktop` است. `App.tsx` یکی از این سه را رندر می‌کند و
شنونده‌های سراسری صفحه‌کلید (کد کونامی، تایپ «دوستت دارم») را نصب می‌کند. پنجره‌ها آبجکت‌هایی در
`useOS.windows` با z-index هستند؛ روی موبایل شیت تمام‌صفحه و روی دسکتاپ کارت قابل کشیدن.

### تم روز و شب

`useNightMode()` بین ساعت ۱۸ تا ۶ حالت شب برمی‌گرداند (یا از تنظیمات دستی پیروی می‌کند) و
`data-theme="night"` را روی `<html>` می‌نویسد. همه‌ی رنگ‌ها متغیر CSS هستند
(`--os-bg`، `--os-card`، `--os-accent` و…) پس کل سیستم‌عامل یک‌جا پالتش عوض می‌شود.

---

## ۳. نصب و راه‌اندازی

### پیش‌نیازها

- پایتون ۳٫۱۱ به بالا
- نود ۲۰ به بالا
- MySQL 8 (فقط پروداکشن — توسعه با SQLite است)

### شروع سریع

```bash
git clone <repo> LoveOS && cd LoveOS
./scripts/dev.sh
```

اسکریپت `dev.sh` محیط مجازی را می‌سازد، همه‌چیز را نصب می‌کند، `.env` را کپی می‌کند، مهاجرت و
داده‌ی اولیه را اجرا می‌کند و هر دو سرور را بالا می‌آورد:

- **اپ دخترم:** http://localhost:5173
- **پنل بابا:** http://localhost:8000/daddy-panel-9x7k/

### راه‌اندازی دستی

```bash
# ---- بک‌اند
cd backend
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
cp .env.example .env            # بعد ویرایشش کن
.venv/bin/python manage.py migrate
.venv/bin/python manage.py seed_loveos --passcode 1234 --vault 0000
.venv/bin/python manage.py createsuperuser
.venv/bin/python manage.py runserver 0.0.0.0:8000

# ---- فرانت‌اند (ترمینال دوم)
cd frontend
npm install
npm run dev -- --host 0.0.0.0
```

### متغیرهای محیطی

| متغیر | پیش‌فرض | معنی |
|---|---|---|
| `DEBUG` | `True` | در پروداکشن حتماً **خاموش**. |
| `DJANGO_SECRET_KEY` | — | رشته‌ی تصادفی بلند. در پروداکشن اجباری. |
| `ALLOWED_HOSTS` | `*` | دامنه‌ها با کاما. |
| `CSRF_TRUSTED_ORIGINS` | `https://*.e2b.app` | برای پنل روی HTTPS لازم است. |
| `TIME_ZONE` | `Asia/Tehran` | منطقه‌ی زمانی سرور. |
| `ADMIN_PATH` | `daddy-panel-9x7k` | مسیر مخفی پنل (بدون اسلش ابتدایی). |
| `ADMIN_GATE_PASSCODE` | *(خالی)* | رمز دوم قبل از لاگین جنگو. خالی یعنی دروازه خاموش. |
| `DB_ENGINE` | `sqlite` | `sqlite` یا `mysql`. |
| `DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_HOST` / `DB_PORT` | — | اتصال MySQL. |
| `NOTIFY_PROVIDER` | `console` | `console` (فقط لاگ) یا `soroush` (ارسال واقعی). |
| `SOROUSH_API_BASE` | `https://api.splus.ir` | ریشه‌ی API بات. |
| `SOROUSH_TOKEN` | — | توکن از `splus.ir/botfather`. |
| `SOROUSH_DADDY_CHAT_ID` | — | آی‌دی چت بابا. |
| `SOROUSH_WEBHOOK_SECRET` | `loveos-hook` | بخش محرمانه‌ی آدرس وب‌هوک. |
| `SOROUSH_PARSE_MODE` | `HTML` | `HTML`، `MarkdownV2` یا `Markdown`. |
| `SESSION_TTL_HOURS` | `720` | مدت باز ماندن نشست (۳۰ روز). |
| `VAULT_SESSION_MINUTES` | `20` | قفل خودکار صندوقچه. |
| `MAX_UNLOCK_ATTEMPTS` | `5` | تعداد اشتباه تا ظاهر شدن دکمه‌ی کمک. |
| `API_RATE_LIMIT` | `300/min` | محدودیت نرخ. |

### داده‌ی اولیه

دستور `manage.py seed_loveos` **تکرارپذیر** است — فقط چیزهای نبوده را می‌سازد: ردیف پیکربندی،
رمزها، ۶ صورت‌فلکی «M-A-R-Y-A-M»، ۴ گل، کوییز، لیست آرزوها، پیام‌های حال دل، درس‌های آموزش،
دستورهای ترمینال، ۱۶ دستاورد، ۱۵ راز و ۳ پازل.

```bash
.venv/bin/python manage.py seed_loveos --passcode 1234 --vault 0000
```

### ساخت QR برای او

```bash
python3 scripts/qr.py https://your-secret-domain.com --out loveos-qr.svg
```

فایل `scripts/qr.py` الگوریتم QR را از صفر پیاده کرده (حالت بایت، سطح تصحیح خطا M، نسخه‌های ۱ تا ۱۰)
و **هیچ وابستگی خارجی** ندارد. کد را در ترمینال چاپ می‌کند و در صورت نیاز یک SVG با رنگ‌های LoveOS
می‌سازد که آماده‌ی چاپ و گذاشتن داخل یک کارت است.

---

## ۴. بیست‌وهفت اپ

بیست‌وچهار تا آیکن دسکتاپ دارند. **یادآورهای مهربان** عمداً آیکن ندارد (فقط در مرکز اعلان زندگی
می‌کند) و **مرکز اعلان** و **درباره** هم بخشی از پوسته‌اند نه آیکن.

### هسته‌ی احساسی

**۱ · نقشه‌ی ما** — `map`
دو سنجاق قلبی روی نقشه‌ی OpenStreetMap، یک کمان خط‌چین بینشان، ساعت محلی زنده و نشان شب/روز برای هر
شهر، فاصله‌ی هوایی به کیلومتر و دکمه‌ی «پرواز روی مسیر» که دوربین را از شهر بابا به شهر او و بعد
به نمای کلی می‌برد. پایینش جمله‌ی بابا: *«فاصله فقط یه عدده. قلبمون همیشه یکیه.»*

**۲ · صندوق صدا** — `voice`
ویس‌های بابا، دسته‌بندی‌شده بر اساس حال دل او: صبح‌ها، شب‌ها، وقتی دلت تنگه، وقتی خوشحالی، وقتی
ناراحتی، وقتی نمی‌تونی بخوابی. دکمه‌ی «یه ویس تصادفی» یکی را شانسی می‌آورد. هر پخش به بابا خبر
می‌دهد. آپلود **فقط دست بابا**ست.

**۳ · موسیقی ما** — `music`
آهنگ اصلی بالای صفحه با توضیح «چرا این آهنگ؟»، متن ترانه و کاور. پایینش لیست پخش، تایمر خواب
(۱۰/۲۰/۳۰/۴۵ دقیقه و بعد همه‌چیز خاموش) و — چون خودش خواست — **دخترم هم می‌تواند آهنگ آپلود کند**.
آهنگ‌های او با برچسب «از من» می‌آیند، فقط مال خودش را می‌تواند پاک کند، به بابا خبر می‌رود و در پنل
هم مشخص است چه کسی آپلود کرده. بابا می‌تواند با `allow_daughter_music_upload` این اجازه را ببندد.

**۴ · خاطره‌ها** — `memories`
خط زمان عمودی با عکس، متن، مکان و ویس اختیاری. خاطره‌های آینده به شکل `؟؟؟` با قفل و تاریخ «باز
می‌شه در …» دیده می‌شوند — دستور `sweep` سر وقت بازشان می‌کند و به او خبر می‌دهد. حالت اسلایدشو هم
عکس‌ها را نرم پشت‌سرهم نشان می‌دهد.

**۵ · نامه‌های نجوا** — `whisper`
پاکت‌های کاغذی روی قفسه. با زدن روی هرکدام با چرخش سه‌بعدی، صدای خش‌خش کاغذ و خط نستعلیق باز می‌شود.
نامه‌های زمان‌دار تا تاریخشان مهروموم می‌مانند. «یه نامه تصادفی برای امروز» یکی را انتخاب می‌کند.
باز شدن هر نامه به بابا خبر می‌دهد.

**۶ · شمارش معکوس** — `countdown`
شمارش زنده تا دیدار بعدی، تولدها و سالگردها — روز/ساعت/دقیقه که هر دقیقه تازه می‌شود، هرکدام با
آیکن خودش و یک پیام جشن وقتی به صفر برسد.

**۷ · آب‌وهوای ما** — `weather`
هوای دو شهر کنار هم با آسمان متحرک (خورشید شناور، باران، برف)، دما، رطوبت، باد، طلوع و غروب. وقتی
دو دما به هم نزدیک باشند می‌گوید *«امروز هوای جفتمون یه شکله؛ انگار کنار همیم.»* داده از
Open-Meteo (بدون کلید API).

**۸ · ضربان قلب** — `heartbeat`
یک قلب بزرگ که می‌تپد، با صدای سنتزی «لاب-داب» و یک لرزش کوتاه هماهنگ روی هر ضربان. انگشتت را
نگه داری ضربان از ۷۲ تا حدود ۱۲۲ بالا می‌رود؛ رها کنی آرام می‌شود.

**۹ · باغچه‌ی ما** — `garden`
چهار گل که با SVG رسم می‌شوند — ساقه، برگ، گلبرگ — در پنج مرحله‌ی رشد. با آب دادن گل بزرگ‌تر می‌شود،
صدای شکوفه پخش می‌شود و یکی از پیام‌هایی که بابا نوشته ظاهر می‌شود.

**۱۰ · آسمان ستاره‌ها** — `starmap`
اسم او با صورت‌های فلکی روی آسمان سرمه‌ای و غبار ستاره‌ای چشمک‌زن نوشته شده. روی هر حرف بزنی
ستاره‌هایش طلایی روشن می‌شوند و جمله‌ی بابا برای آن حرف می‌آید؛ «کل اسم رو روشن کن» همه را با هم
روشن می‌کند.

**۱۱ · ترمینال عشق** — `terminal`
یک شل بامزه: `help`، `whoami`، `ls /heart`، `cd /us`، `love --status`، `heartbeat --live`،
`distance --km`، `music --list`، `play voice --random`، `memory --random`، `future --countdown`،
`clear`، `exit`. دستور `sudo kiss` یک رمز الکی می‌پرسد و بعد بوسه می‌بارد؛ `sudo hug --force` یک بغل
واقعی با لرزش می‌سازد. بعضی دستورها اپ‌های دیگر را باز می‌کنند. دستورهای دلخواه و خروجی‌شان در پنل
قابل ویرایش‌اند. کلیدهای بالا و پایین تاریخچه را مرور می‌کنند.

**۱۲ · چت با بابا** — `chat`
پیام‌های او مستقیم به سروش بابا می‌رود؛ جواب بابا از وب‌هوک می‌آید و به شکل حباب با برچسب «از سروش»
می‌نشیند. هر ۶ ثانیه بررسی می‌شود.

**۱۳ · حال دلم** — `mood`
شش حال به شکل دکمه‌های ایموجی بزرگ. انتخاب هرکدام به بابا خبر می‌دهد و پیام **و ویسی** را که بابا
دقیقاً برای همان حال ضبط کرده برمی‌گرداند.

**۱۴ · کوییز ما** — `quiz`
«چقدر بابا رو می‌شناسی؟» — سؤال‌ها یکی‌یکی با نوار پیشرفت، بعد نمره، مرور هر سؤال با توضیح، و یک
پیام جایزه‌ی مخفی برای نمره‌ی کامل.

**۱۵ · آرزوهای ما** — `plans`
لیست آرزوهای مشترک در چهار دسته (سفر، کار، آرزو، خانه). او می‌تواند اضافه کند و تیک بزند؛ هر تیک
به بابا خبر می‌دهد. فقط مال خودش را می‌تواند پاک کند.

**۱۶ · سینمای ما** — `cinema`
فیلم‌ها و سریال‌هایی که با هم می‌بینند: وضعیت (ندیدم / در حال تماشا / دیدم)، امتیاز پنج‌ستاره، لینک
تماشا و پوستر.

**۱۷ · صندوقچه** — `vault`
یک رمز دوم از عکس‌ها، ویس‌ها، ویدیوها و یادداشت‌هایی که فقط مال اوست محافظت می‌کند. بعد از ۲۰ دقیقه
خودکار قفل می‌شود.

**۱۸ · بغل** — `hug`
دوطرفه، **هر دو جهت با لرزش**:
- *او → بابا:* دکمه‌ی قلب بزرگ یک بغل می‌فرستد، گوشی‌اش می‌لرزد، صدای ضربان پخش می‌شود، صفحه در یک
  رنگ گرم غرق می‌شود و همان لحظه به سروش بابا پیام می‌رود.
- *بابا → او:* بغل بابا به شکل یک کارت منتظر می‌ماند؛ با باز کردنش گوشی با الگوی تنظیم‌شده در پنل
  می‌لرزد (پیش‌فرض `۲۰۰ / ۱۰۰×۳ / ۲۰۰`)، صدای ضربان پخش می‌شود و هاله‌ی گرم می‌تابد. به بابا خبر
  می‌رود که دخترش بغلش را حس کرد.

شمارنده‌ی هر دو جهت دیده می‌شود. سه بغل پشت‌سرهم یک راز را باز می‌کند و به بابا می‌گوید دخترت خیلی
دلش تنگ شده.

**۱۹ · پازل قلب** — `puzzle`
یک عکس به قطعات ۳×۳، ۴×۴ یا ۵×۵ تقسیم می‌شود؛ با جابه‌جا کردن دو قطعه مرتب می‌شود. زمان‌سنج، شمارش
حرکت، بهترین رکورد و تعداد محدودی کمک (یک نگاه کوتاه به عکس کامل). با حل شدن، پیام و ویس بابا پخش
می‌شود و زمانش برای بابا فرستاده می‌شود.

**۲۰ · نشان‌ها** — `achievements`
شانزده دستاورد با نوار پیشرفت. قفل‌ها خاکستری‌اند؛ با باز شدن، پیام مخفی‌ای که بابا برایش نوشته
آشکار می‌شود.

### مراقبت و روزمرگی

**۲۱ · چرخه و مراقبت** — `cycle` *(حساس)*
پنج زبانه:
- **تقویم** — ثبت شروع و پایان پریود؛ شبکه‌ی شش‌هفته‌ای روزهای واقعی، پیش‌بینی‌شده و تخمک‌گذاری را
  نشان می‌دهد.
- **علائم** — هشت مقیاس ۰ تا ۵ (حال، انرژی، سردرد، کمردرد، دل‌درد، تهوع، خواب، اشتها) به‌علاوه‌ی
  یادداشت. روزهای سخت بی‌سروصدا به بابا می‌گویند یه پیام قشنگ برایش بفرستد.
- **داروها** — نوبت‌های امروز با **خوردم / بعداً یادآوری کن / نمی‌تونم بخورم**؛ هر انتخاب یک رویداد
  جدا در سروش دارد.
- **گزارش‌ها** — درصد پایبندی ۳۰ روزه و نمودار میله‌ای طول چرخه‌ها که میله‌های خارج از محدوده قرمز
  می‌شوند.
- **مراقبت از خودم** — کلیدهای مهربان (آب بخور، کش‌وقوس، استراحت) از پنل.

همه‌ی تحلیل‌ها **فقط** از تاریخچه‌ی خودش محاسبه می‌شوند. اگر طول چرخه از بازه‌ی ۲۱ تا ۳۵ روز خارج
شود اپ تذکر می‌دهد و به بابا هم می‌گوید یادش بیندازد به پزشک سر بزند. روی همه‌ی صفحه‌ها نوشته:
*«این اپ جایگزین مشاوره پزشک نیست.»* در سمت پنل، این بخش **رمز تأیید دوم** می‌خواهد و هر بار دیدن
اطلاعات در `HealthAccessLog` ثبت می‌شود. بابا دید کامل دارد — حالت خصوصی وجود ندارد.

**۲۲ · کتابخونه‌ی ما** — `library`
قفسه‌ای از کتاب‌ها. خواندن صفحه‌به‌صفحه با ورق خوردن سه‌بعدی، بافت کاغذ، اندازه‌ی متن قابل تنظیم،
عکس و صدای اختیاری برای هر صفحه، نشانک و حاشیه‌نویسی (دوبار زدن روی پاراگراف). خودش هم می‌تواند
فصل بنویسد — اول **پیش‌نویس**، بعد **انتشار** که به بابا خبر می‌دهد. رنگ متن بر اساس نویسنده فرق
می‌کند و او فقط نوشته‌های خودش را می‌تواند ویرایش کند.

**۲۳ · یادآورهای مهربان** *(بدون آیکن دسکتاپ)*
فقط داخل مرکز اعلان و در زبانه‌ی خودش زندگی می‌کند. یادآورها هم از ورودی‌های دستی بابا می‌آیند و هم
از قانون‌های خودکار در `sweep`: بر اساس هوا («چتر یادت نره»، «خودت رو گرم نگه دار»، «آب بیشتری
بخور»، «یه چای گرم بخور»)، بر اساس چرخه، سالگرد، تولد و گاهی یک محبت تصادفی. قانون‌های سفت:
**هیچ یادآور روزانه‌ای نیست** و **حداکثر ۵ یادآور فعال** هم‌زمان. او می‌تواند هرکدام را بی‌صدا کند و
به بابا خبر می‌رود که دیده شده.

### سیستمی

**۲۴ · آموزش** — `tutorial`
درس‌های مهربان به قلم بابا — سیستم‌عامل یعنی چی، اپ چیه، دسکتاپ و داک، ترمینال چیه، کتابخونه چطور
کار می‌کنه و…. دکمه‌ی **کمک** در نوار عنوان هر اپ، دقیقاً درس همان اپ را باز می‌کند. **هیچ اشاره‌ای
به رازها در این اپ نیست.**

**۲۵ · تنظیمات** — `settings`
زبان (فارسی/انگلیسی)، تم (خودکار/روز/شب)، صدا، لرزش، اندازه‌ی فونت (۸۵٪ تا ۱۴۰٪)، اجازه‌ی اعلان،
پیشنهاد نصب روی گوشی، درباره و خروج. تنظیمات روی سرور ذخیره می‌شوند تا روی هر دستگاهی همراهش باشند.

**۲۶ · مرکز اعلان** *(داک)*
همه‌ی اعلان‌های سیستم — نامه‌ی تازه، خاطره‌ی باز شده، نشان، بغل، چت، دارو — به‌علاوه‌ی زبانه‌ی
یادآورهای مهربان. نشان تعداد نخوانده روی زنگوله‌ی داک، «همه رو خوندم» و با زدن روی هر اعلان اپ
مربوطه باز می‌شود. هر ۴۵ ثانیه بررسی می‌شود.

**۲۷ · درباره LoveOS** *(منوی شروع)*
لوگو، نسخه، متنی از بابا، اسم هر دو و تعداد روزهایی که با هم بوده‌اند.

---

## ۵. پنل بابا

### ورود

```
https://your-domain.com/<ADMIN_PATH>/        مثلاً /daddy-panel-9x7k/
```

دو لایه:
۱. **دروازه** (`AdminGateMiddleware`) — یک صفحه‌ی صورتی کوچک که `ADMIN_GATE_PASSCODE` را می‌پرسد.
   قبل از CSRF اجرا می‌شود تا فرم ساده کار کند و نتیجه را در نشست نگه می‌دارد.
۲. **لاگین جنگو** — همان سوپریوزری که با `createsuperuser` ساختی.

همه‌ی پاسخ‌ها هدر `X-Robots-Tag: noindex, nofollow, noarchive` دارند.

### چه چیزهایی را کنترل می‌کند

| بخش | مدل‌ها | چه چیزی را می‌چرخاند |
|---|---|---|
| **پیکربندی** | `UserConfig` | اسم‌ها، لقب، شهرها و مختصات، منطقه‌ی زمانی، سالگرد، تولد، دیدار بعدی، پیام بوت، پیام رمز اشتباه، پیام کمک، سؤال و جواب امنیتی، پیام امروز، متن درباره، لوگو، هر چهار پس‌زمینه، زبان، تم، صدا، اندازه‌ی فونت، رمزها و `allow_daughter_music_upload`. |
| **محتوا** | `Voice`، `Song`، `Memory`، `Letter`، `Countdown`، `Flower`، `FlowerMessage`، `Constellation`، `CinemaItem`، `QuizQuestion`، `QuizReward`، `FuturePlan`، `MoodMessage`، `VaultItem`، `TutorialChapter`، `TerminalCommand` | هر کلمه و هر فایلی که او می‌بیند. |
| **اجتماعی** | `ChatMessage`، `Hug`، `HugSettings`، `Reminder`، `ReminderLog` | تاریخچه‌ی چت، فرستادن بغل، الگوی لرزش و رنگ گرم، یادآورهای مهربان. |
| **سلامت** | `CycleEntry`، `SymptomLog`، `Medication`، `MedicationLog`، `CareReminder`، `HealthAccessLog` | پشت رمز دوم؛ هر دسترسی ثبت می‌شود. |
| **کتابخانه** | `Book`، `Chapter`، `Page`، `Paragraph`، `MarginNote`، `Bookmark` | نوشتن کتاب با او؛ `allow_daughter_edit` برای هر کتاب. |
| **بازی** | `Puzzle`، `PuzzleRecord` | عکس پازل، سختی، تعداد کمک، پیام و ویس پایانی. |
| **هسته** | `OSNotification`، `SoroushOutbox`، `ActivityLog`، `Achievement`، `AchievementUnlock`، `Counter`، `EasterEgg`، `EasterEggLog` | اعلان‌ها، صف سروش، فعالیت‌های او، نشان‌ها و متن رازها. |

### دستورالعمل‌های روزمره

**همین الان بغلش کن** — اجتماعی ← بغل‌ها ← افزودن، جهت `daddy_to_daughter`، متن را بنویس و ذخیره کن.
در اپ بغلِ او ظاهر می‌شود و موقع باز کردن می‌لرزد.

**یک ویس برای یک حال ضبط کن** — محتوا ← ویس‌ها ← فایل را آپلود کن، بعد محتوا ← پیام‌های حال دل ←
آن حال را انتخاب کن ← همان ویس را بچسبان. دفعه‌ی بعد که آن حال را بزند، صدای بابا را می‌شنود.

**یک نامه برای تولدش بنویس** — محتوا ← نامه‌ها ← افزودن، `open_at` را تنظیم کن. تا آن لحظه مهروموم
می‌ماند، بعد `sweep` بازش می‌کند و به او خبر می‌دهد.

**متن یک راز را عوض کن** — هسته ← رازها ← تریگر را انتخاب کن ← عنوان/پیام/پیوست را ویرایش کن.
منطق تریگر در کد است؛ هر کلمه‌اش مال بابا.

**آپلود موسیقی او را ببند** — پیکربندی ← تیک `allow_daughter_music_upload` را بردار.

---

## ۶. اتصال به بات سروش

### راه‌اندازی

۱. در سروش‌پلاس با `@botfather` حرف بزن و یک بات بساز؛ توکنش را بردار.
۲. `SOROUSH_TOKEN`، `SOROUSH_DADDY_CHAT_ID` و `SOROUSH_WEBHOOK_SECRET` را در `.env` بگذار و
   `NOTIFY_PROVIDER=soroush` کن.
۳. وب‌هوک را ثبت کن (فقط پورت‌های ۴۴۳، ۸۰، ۸۸ و ۸۴۴۳ مجازند):

```bash
curl "https://api.splus.ir/bot<TOKEN>/setWebhook?url=https://your-domain.com/api/soroush/webhook/<SECRET>/"
curl "https://api.splus.ir/bot<TOKEN>/getWebhookInfo"
```

این API سازگار با تلگرام است: `https://api.splus.ir/bot<token>/METHOD` و هر پاسخ به شکل
`{ok, result | description}` برمی‌گردد.

### حدود ۲۰ رویدادی که بابا دریافت می‌کند

| رویداد | کِی می‌آید |
|---|---|
| `login` | وارد LoveOS می‌شود. |
| `lock_help` | بعد از ۵ رمز اشتباه، «کمک از بابا» را می‌زند. |
| `forgot_ok` | با سؤال امنیتی وارد می‌شود. |
| `voice_played` | به یکی از ویس‌های بابا گوش می‌دهد. |
| `letter_opened` | یک نامه را باز می‌کند. |
| `memory_unlocked` | یک خاطره‌ی آینده برایش باز می‌شود. |
| `song_uploaded` | یک آهنگ آپلود می‌کند. |
| `mood` | حال دلش را ثبت می‌کند. |
| `chat` | پیام چت می‌فرستد. |
| `hug` | بابا را بغل می‌کند. |
| `hug_seen` | بغل بابا را باز می‌کند. |
| `hug_miss` | سه بغل پشت‌سرهم — خیلی دلش تنگ شده. |
| `quiz_perfect` | نمره‌ی کامل کوییز. |
| `puzzle` | پازل را حل می‌کند (با زمانش). |
| `plan_add` / `plan_done` | آرزو اضافه می‌کند یا تیک می‌زند. |
| `cinema_add` | فیلم یا سریال اضافه می‌کند. |
| `book_new` / `book_chapter` / `book_comment` | کتاب می‌سازد، فصل منتشر می‌کند یا حاشیه می‌نویسد. |
| `period_start` / `period_end` | شروع یا پایان پریود را ثبت می‌کند. |
| `cycle_anomaly` | طول چرخه‌اش از حالت عادی خارج می‌شود. |
| `symptoms` | یک روز سخت را ثبت می‌کند (شدت ۴ به بالا). |
| `med_taken` / `med_skipped` / `med_added` | کارهای دارویی. |
| `reminder_sent` / `reminder_viewed` | یادآور فرستاده شد / او دیدش. |
| `achievement` | یک نشان باز می‌کند. |
| `easter_egg` | یک راز پیدا می‌کند. |

### تضمین تحویل

تابع `notify_daddy()` پیام را در `SoroushOutbox` می‌نویسد و بلافاصله تلاش به ارسال می‌کند. اگر شبکه
یا توکن نبود، در صف می‌ماند؛ `manage.py sweep` هر دقیقه `flush_outbox()` را صدا می‌زند و دوباره
تلاش می‌کند. با `NOTIFY_PROVIDER=console` هیچ‌چیز از دستگاه بیرون نمی‌رود — فقط لاگ می‌شود، که همان
حالت محیط توسعه است.

### جواب دادن بابا

وقتی بابا به بات جواب می‌دهد، سروش یک `Update` به
`/api/soroush/webhook/<secret>/` پست می‌کند. هندلر ابتدا رمز مسیر را چک می‌کند، بعد با
`SOROUSH_DADDY_CHAT_ID` فیلتر می‌کند، متن را به‌عنوان `ChatMessage` با `via="soroush"` ذخیره می‌کند و
یک اعلان می‌سازد. کال‌بک دکمه‌های این‌لاین هم همیشه با `answerCallbackQuery` پاسخ داده می‌شوند، همان‌طور
که مستند API الزام کرده.

---

## ۷. مرجع API

پایه: `/api`. همه‌چیز به‌جز `/boot`، `/auth/unlock`، `/auth/forgot` و `/auth/help` به هدر
`Authorization: Token <token>` نیاز دارد.

### پوسته و احراز هویت

| متد | مسیر | کار |
|---|---|---|
| GET | `/boot` | پیکربندی عمومی که بوت و قفل را می‌چرخاند. |
| POST | `/auth/unlock` | `{passcode}` ← `{ok, token}` یا یک پیام شکست بامزه. |
| POST | `/auth/forgot` | `{answer}` ← توکن، اگر جواب امنیتی درست باشد. |
| POST | `/auth/help` | خبر دادن به بابا از راه سروش. |
| POST | `/auth/logout` | پایان نشست. |
| GET | `/me` | پیکربندی کامل برای نشست باز. |
| PATCH | `/settings` | زبان، تم، صدا، اندازه‌ی فونت. |
| POST | `/vault/unlock` | `{passcode}` ← صندوقچه را ۲۰ دقیقه باز می‌کند. |

پاسخ `GET /boot`:

```json
{
  "config": {
    "daughter_name": "مریم", "daughter_nickname": "دخترم", "daddy_name": "بابا",
    "days_together": 365,
    "next_meeting": "2026-10-29T07:31:46Z",
    "next_meeting_delta": { "days": 44, "hours": 23 },
    "boot_greeting": "سلام دخترم...",
    "wrong_pass_message": "...", "lock_help_message": "...", "security_question": "...",
    "language": "fa", "theme": "auto", "sound_enabled": true, "font_scale": 1.0,
    "logo": null, "boot_background": null, "lock_background": null,
    "desktop_background_day": null, "desktop_background_night": null,
    "is_birthday": false, "is_anniversary": false, "has_passcode": true
  },
  "unlocked": false
}
```

### محتوا

```
GET    /voices                     GET    /voices/random
POST   /voices/<id>/played
GET    /songs                      POST   /songs/upload      (multipart)
DELETE /songs/<id>                 POST   /songs/<id>/played
GET    /memories
GET    /letters                    GET    /letters/random
POST   /letters/<id>/open
GET    /countdowns
GET    /garden                     POST   /garden/<id>/water
GET    /starmap
GET    /cinema                     POST   /cinema
PATCH  /cinema/<id>                DELETE /cinema/<id>
GET    /quiz                       POST   /quiz/submit
GET    /plans                      POST   /plans
PATCH  /plans/<id>                 DELETE /plans/<id>
GET    /moods                      POST   /moods/set
GET    /vault
GET    /tutorial[?key=...]
```

### اجتماعی

```
GET    /chat[?since=<id>]          POST   /chat
GET    /hug                        POST   /hug/send          POST /hug/<id>/open
GET    /notifications              POST   /notifications/read-all
POST   /notifications/<id>/read
GET    /reminders                  POST   /reminders/<id>/mute
POST   /terminal                   POST   /terminal/sudo
POST   /egg                        GET    /achievements
GET    /weather                    GET    /map
```

### سلامت

```
GET    /cycle                      POST   /cycle/start       POST /cycle/end
GET    /cycle/symptoms             POST   /cycle/symptoms
GET    /meds                       POST   /meds
GET    /meds/today                 POST   /meds/act
GET    /meds/report
GET    /care                       POST   /care/<id>/toggle
```

### کتابخانه و بازی

```
GET    /books                      POST   /books
GET    /books/<id>                 POST   /books/<id>/chapters
POST   /chapters/<id>/publish
POST   /pages/<id>/paragraphs      POST   /pages/<id>/bookmark
PATCH  /paragraphs/<id>            DELETE /paragraphs/<id>
POST   /paragraphs/<id>/notes
GET    /puzzles                    POST   /puzzles/<id>/start
POST   /puzzles/<id>/complete
```

### بیرون از `/api`

```
POST   /api/soroush/webhook/<secret>/     آپدیت‌های سروش
GET    /robots.txt                        Disallow: /
GET    /healthz                           "ok"
ANY    /<ADMIN_PATH>/                     پنل بابا
```

---

## ۸. رازها

> **این بخش فقط برای باباست.** در اپ آموزش، صفحه‌ی درباره و هیچ جای دیگری **حتی یک اشاره** به وجود
> رازها نیست. او فقط باید تصادفی به آن‌ها بربخورد.

*منطق تریگر* در کد است؛ اما *عنوان، پیام و پیوست* هر راز در هسته ← رازها نگهداری می‌شود تا بابا هر
وقت خواست بازنویسی‌شان کند. پیدا کردن هر راز باعث باریدن قلب روی صفحه، یک لرزش ملایم و خبر دادن به
بابا می‌شود.

| # | کلید | چطور پیدایش می‌کند |
|---|---|---|
| ۱ | `click_logo_5` | پنج بار زدن روی لوگو در صفحه‌ی بوت. |
| ۲ | `type_love` | تایپ «دوستت دارم» (یا «i love you») هر جای سیستم. |
| ۳ | `konami` | ↑ ↑ ↓ ↓ ← → ← → B A روی صفحه‌کلید. |
| ۴ | `terminal_rm` | دستور `sudo rm -rf /loneliness` در ترمینال. |
| ۵ | `terminal_sandwich` | دستور `sudo make me a sandwich`. |
| ۶ | `midnight` | باز کردن LoveOS بین ۰۰:۰۰ تا ۰۵:۰۰ — آسمان پر از قلب و ستاره می‌شود. |
| ۷ | `birthday` | روز تولدش یک آیکن کیک روی دسکتاپ ظاهر می‌شود. |
| ۸ | `anniversary` | روز سالگردشان یک قلب تپنده ظاهر می‌شود. |
| ۹ | `map_zoom` | زوم کامل روی شهر بابا در نقشه. |
| ۱۰ | `star_double_click` | دو بار زدن روی یک ستاره در آسمان ستاره‌ها. |
| ۱۱ | `garden_5_water` | پنج بار آب دادن به یک گل. |
| ۱۲ | `hug_3` | سه بغل پشت‌سرهم (به بابا هم «دلش تنگ شده» می‌رسد). |
| ۱۳ | `chat_love` | نوشتن «دوستت دارم» در چت. |
| ۱۴ | `music_3_play` | سه بار پخش آهنگ اصلی. |
| ۱۵ | `lock_5_wrong` | پنج بار اشتباه زدن رمز. |

هر کشف در `EasterEggLog` ثبت می‌شود، شمارنده‌ی `eggs_found` را بالا می‌برد و نشان‌های
`secret_finder` و `secret_master` را باز می‌کند.

### نشان‌ها (۱۶ تا)

`first_login`، `first_voice`، `days_100`، `quiz_perfect`، `all_letters`، `puzzle_player`،
`puzzle_master`، `hug_bunny`، `kind_daughter`، `med_regular`، `self_care`، `writer`،
`storyteller`، `secret_finder`، `secret_master`، `know_it_all`.

---

## ۹. استقرار روی سرور

### با یک دستور

```bash
sudo APP_DIR=/srv/loveos DOMAIN=loveos.example.com ./scripts/deploy.sh
```

کد را می‌کشد، نصب می‌کند، فرانت را build می‌کند، مهاجرت می‌زند، استاتیک‌ها را جمع می‌کند، یونیت
systemd و سایت Nginx را می‌نویسد، کرون را نصب می‌کند و همه‌چیز را ری‌لود می‌کند.

### چک‌لیست پروداکشن

- [ ] `DEBUG=False` و یک `DJANGO_SECRET_KEY` تصادفی و بلند
- [ ] `ALLOWED_HOSTS` روی دامنه‌ی واقعی
- [ ] `ADMIN_PATH` از حالت پیش‌فرض عوض شده باشد
- [ ] `ADMIN_GATE_PASSCODE` تنظیم شده باشد
- [ ] `DB_ENGINE=mysql` با `utf8mb4` و نصب `mysqlclient`
- [ ] گواهی HTTPS (`certbot --nginx -d your-domain.com`)
- [ ] وب‌هوک سروش ثبت شده و `getWebhookInfo` تمیز باشد
- [ ] کرون نصب باشد: `* * * * * cd /srv/loveos/backend && .venv/bin/python manage.py sweep`
- [ ] از `media/` بکاپ گرفته شود (صدای بابا آن‌جاست)
- [ ] QR از `scripts/qr.py` چاپ شده باشد

### MySQL

```sql
CREATE DATABASE loveos CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'loveos'@'localhost' IDENTIFIED BY 'strong-password';
GRANT ALL PRIVILEGES ON loveos.* TO 'loveos'@'localhost';
FLUSH PRIVILEGES;
```

### Celery اختیاری

اگر ردیس در دسترس بود، به‌جای کرون:

```bash
celery -A config worker -l info
celery -A config beat -l info
```

مسیر کرون پیش‌فرض است چون به هیچ سرویس اضافه‌ای نیاز ندارد.

---

## ۱۰. نگهداری و رفع اشکال

### کارهای دوره‌ای

| کار | دستور |
|---|---|
| بکاپ دیتابیس | `mysqldump loveos > backup-$(date +%F).sql` |
| بکاپ رسانه‌ها | `tar czf media-$(date +%F).tar.gz backend/media` |
| بررسی صف سروش | پنل ← هسته ← صندوق خروجی سروش (`sent=False` یعنی هنوز نرفته) |
| اجرای دستی زمان‌بند | `.venv/bin/python manage.py sweep` |
| بررسی‌های استقرار | `.venv/bin/python manage.py check --deploy` |
| تست سلامت | `curl https://your-domain.com/healthz` ← `ok` |

### مشکلات رایج

**نمی‌تواند وارد شود.** رمز را ریست کن:

```bash
.venv/bin/python manage.py shell -c "
from accounts.models import UserConfig
c = UserConfig.get_solo(); c.set_passcode('1234'); c.save()"
```

**پیام‌های سروش نمی‌رسند.** اول `NOTIFY_PROVIDER=soroush` را چک کن، بعد
`curl https://api.splus.ir/bot<TOKEN>/getMe` و بعد در `SoroushOutbox` متن خطای ذخیره‌شده را بخوان.
یادت باشد حالت `console` فقط لاگ می‌کند.

**جواب‌های بابا در چت نمی‌آیند.** `getWebhookInfo` را بگیر و `last_error_message` را بخوان. آدرس
باید HTTPS روی پورت ۴۴۳/۸۰/۸۸/۸۴۴۳ باشد، رمز مسیر با `SOROUSH_WEBHOOK_SECRET` یکی باشد و
`SOROUSH_DADDY_CHAT_ID` آی‌دی واقعی بابا باشد (اگر نخواند، پیام بی‌صدا نادیده گرفته می‌شود).

**کارهای زمان‌بندی‌شده اجرا نمی‌شوند.** کرون نصب نیست — `crontab -l | grep sweep`. دستور
`manage.py sweep` را دستی بزن و خط خلاصه‌ای که چاپ می‌کند را بخوان.

**سرویس‌ورکر نسخه‌ی قدیمی را نشان می‌دهد.** `registerType` روی `autoUpdate` است، پس معمولاً یک
رفرش کافی است؛ وگرنه داده‌های سایت را پاک کن. Nginx طوری تنظیم شده که `/sw.js` را هرگز کش نکند.

**آب‌وهوا «—» نشان می‌دهد.** Open-Meteo در دسترس نبوده؛ اپ بدون خطا رد می‌شود و هر ۱۵ دقیقه دوباره
تلاش می‌کند. خروجی HTTPS سرور را چک کن.

**لرزش کار نمی‌کند.** `navigator.vibrate` روی سافاری iOS پشتیبانی نمی‌شود — محدودیت خود پلتفرم
است. بقیه‌ی چیزها (هاله‌ی گرم، صدای ضربان، پیام) سر جایشان کار می‌کنند.

### کوئری‌های مفید

```bash
# دخترم چه کارهایی کرده؟
.venv/bin/python manage.py shell -c "
from core.models import ActivityLog
[print(a.created_at, a.title, a.detail) for a in ActivityLog.objects.all()[:20]]"

# کدوم رازها رو پیدا کرده؟
.venv/bin/python manage.py shell -c "
from core.models import EasterEggLog
[print(l.created_at, l.egg.trigger) for l in EasterEggLog.objects.all()]"

# صف سروش رو به زور خالی کن
.venv/bin/python manage.py shell -c "
from core.soroush import flush_outbox; print(flush_outbox())"
```

---

<div align="center">

**LoveOS نسخه‌ی ۱.۰**

*ساخته‌ی بابا، برای دخترش.*
*فاصله فقط یه عدده. قلبمون همیشه یکیه.* ❤

</div>

</div>
